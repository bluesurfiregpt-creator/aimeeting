/* pages/webview/webview.wxss — v1.4.0 Saga F (iOS 浅色) */

page {
  background: #F2F2F7;
}

.root {
  width: 100vw;
  height: 100vh;
  background: #F2F2F7;
}

/* web-view 容器 默认 白底 — 跟 page bg 一致避免加载瞬间断层 */
web-view {
  background: #F2F2F7;
}
