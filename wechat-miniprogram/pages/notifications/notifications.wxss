/* pages/notifications/notifications.wxss — v1.4.0 Saga F「通知」原生页 (iOS 浅色) */

page {
  background: #F2F2F7;
  color: #1C1C1E;
  font-size: 28rpx;
  height: 100vh;
}

.page {
  display: flex;
  flex-direction: column;
  height: 100vh;
  box-sizing: border-box;
}

/* ============ 顶部 全部已读 条 ============ */
.topbar {
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 24rpx 32rpx;
}
.topbar-count {
  font-size: 26rpx;
  color: #3C3C43;
}
.mark-all {
  padding: 12rpx 28rpx;
  background: rgba(0, 122, 255, 0.10);
  border: 2rpx solid rgba(0, 122, 255, 0.30);
  border-radius: 999rpx;
  color: #007AFF;
  font-size: 24rpx;
  transition: transform 0.15s ease, background 0.15s ease;
}
.mark-all-hover {
  transform: scale(0.96);
  background: rgba(0, 122, 255, 0.18);
}
.mark-all-busy {
  opacity: 0.55;
}

/* ============ Body ============ */
.body {
  flex: 1;
  overflow-y: auto;
  padding: 0 32rpx;
}

/* ============ Skeleton ============ */
.sk {
  background: #FFFFFF;
  border-radius: 24rpx;
  margin-bottom: 16rpx;
  animation: sk-pulse 1.4s ease-in-out infinite;
}
.sk:first-child { margin-top: 8rpx; }
.sk-row { height: 130rpx; }
@keyframes sk-pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}

/* ============ Empty / Error ============ */
.empty-card {
  margin-top: 64rpx;
  padding: 72rpx 32rpx;
  border: 2rpx dashed #D1D1D6;
  border-radius: 24rpx;
  text-align: center;
}
.empty-emoji { font-size: 64rpx; line-height: 1; }
.empty-title {
  margin-top: 24rpx;
  font-size: 30rpx;
  color: #1C1C1E;
}
.empty-body {
  margin-top: 12rpx;
  font-size: 24rpx;
  color: #8E8E93;
  line-height: 1.6;
}
.centered {
  padding: 100rpx 32rpx;
  text-align: center;
}
.centered-title { font-size: 32rpx; color: #1C1C1E; }
.centered-body { margin-top: 12rpx; font-size: 26rpx; color: #8E8E93; }
.btn-ghost {
  margin-top: 32rpx;
  display: inline-block;
  padding: 20rpx 48rpx;
  background: #FFFFFF;
  border: 2rpx solid #D1D1D6;
  border-radius: 24rpx;
  color: #1C1C1E;
  font-size: 28rpx;
}

/* ============ 通知卡 ============ */
.notif-card {
  display: flex;
  align-items: flex-start;
  gap: 8rpx;
  background: #FFFFFF;
  border-radius: 20rpx;
  padding: 24rpx 24rpx 24rpx 8rpx;
  margin-bottom: 12rpx;
  transition: transform 0.15s ease, background 0.15s ease;
  box-shadow: 0 1rpx 3rpx rgba(0, 0, 0, 0.04);
}
.notif-hover {
  transform: scale(0.99);
  background: #F7F7F9;
}
.notif-unread {
  background: rgba(0, 122, 255, 0.04);
}

.notif-dot-col {
  width: 24rpx;
  flex-shrink: 0;
  display: flex;
  justify-content: center;
  padding-top: 14rpx;
}
.unread-dot {
  width: 14rpx;
  height: 14rpx;
  border-radius: 50%;
}
.dot-zinc   { background: #8E8E93; }
.dot-amber  { background: #FF9F0A; }
.dot-rose   { background: #FF3B30; }
.dot-violet { background: #5E5CE6; }

.notif-emoji {
  flex-shrink: 0;
  font-size: 36rpx;
  line-height: 1.2;
  padding-top: 2rpx;
}
.notif-body {
  flex: 1;
  min-width: 0;
  margin-left: 12rpx;
}
.notif-head {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  gap: 16rpx;
}
.notif-kind {
  font-size: 28rpx;
  font-weight: 500;
  color: #1C1C1E;
}
.notif-when {
  flex-shrink: 0;
  font-size: 22rpx;
  color: #8E8E93;
}
.notif-title {
  margin-top: 8rpx;
  font-size: 26rpx;
  color: #3C3C43;
  line-height: 1.5;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.bottom-pad {
  height: calc(48rpx + env(safe-area-inset-bottom));
}

/* ============ 入场动效 ============ */
@keyframes notif-enter-anim {
  from { opacity: 0; transform: translateY(16rpx); }
  to   { opacity: 1; transform: translateY(0); }
}
.notif-enter {
  animation: notif-enter-anim 0.3s cubic-bezier(0.16, 1, 0.3, 1) both;
}
