# deploy/.env — server-only secrets passed to docker compose at build/run time.
#
# 这个文件**只在服务器上存在**(/opt/aimeeting/deploy/.env),不入 git,
# 也别 rsync 到服务器(rsync 时必须 exclude,否则 --delete 会把它干掉).
#
# 创建方法(在服务器上,首次部署时):
#   cd /opt/aimeeting/deploy
#   cp .env.example .env
#   chmod 600 .env
#   # 把下面占位值改成真实的强密码
#
# deploy.sh 会在启动前 sanity check 这个文件存在 + POSTGRES_PASSWORD 非空.

# Postgres 用户密码(必填,同时是 docker compose `${POSTGRES_PASSWORD}`
# 的解析源).改这个值 = 必须同时跑 ALTER USER aimeeting WITH PASSWORD '...'
# 否则 backend 连不上 DB.
POSTGRES_PASSWORD=replace_me_with_strong_random_48hex

# v24.4 #2 Sentry 配在另一处 — backend/.env 里(env_file 注入 backend 容器),
# 模板见 backend/.env.example 末尾 SENTRY_DSN= 段.
