# v25-6 完整测试报告(全 65 用例 + 162 Cowork)

**测试时间**:2026-05-11 00:30 — 01:00(local)
**测试人**:Claude (Cowork mode, Chrome MCP)
**环境**:https://aimeeting.zhzjpt.cn(底部 build tag `v2026-05-11 00:08:27`)
**对照**:与 v25-5 报告(`docs/测试报告-v25-5-2026-05-10.md`)对照,标注修复 / 回归 / 新发现
**主账号**:`bluesurfiregpt@gmail.com / aimeeting123`
**leader**:`demo.lijg@futian.gov.cn / demo123`
**expert**:`demo.fengl@futian.gov.cn / demo123`

---

## 概要

| 节 | 通过 | 警告 | 失败 | 跳过 | 备注 |
|---|---|---|---|---|---|
| A 一键准备 | 1 | 2 | 0 | 0 | A-3 162 套件可跑 ✅,但 6 fails |
| B 后台 + Audit | 5 | 0 | 0 | 0 | 全过 |
| C leader 视角 | 7 | 1 | 0 | 0 | C-6 AI Kanban 空 |
| D 16 AI | 4 | 1 | 0 | 0 | D-4 chars 偏短 |
| E 会议 + 纪要 | 6 | 0 | 0 | 0 | **E-3、E-2 已修** ✅ |
| F 任务全闭环 | 4 | 1 | 2 | 3 | F-4、F-9 仍坏 |
| G 上级文件 + OCR | 1 | 0 | 0 | 4 | UI 入口 OK,文件上传子例 skip |
| H KB 边界 | 1 | 0 | 0 | 3 | H-4 删除 OK |
| I 数据分级 | 2 | 1 | 0 | 1 | I-3 跨用户 owner 不可见 |
| J 通知 + loop | 3 | 0 | 0 | 0 | 全过 |
| K 基础设施 | 2 | 0 | 0 | 1 | K-3 cowork XX-2 已含 |
| L 演示走查 | 1 | 0 | 1 | 1 | **L-2 ABAC 漏洞**(新发现) |
| M 错误处理 | 3 | 2 | 0 | 0 | M-2/M-3 文案/状态码 |
| **Cowork (A-3)** | 148 | 0 | 6 | 8 | 全自动化 162 套件 |

**已测 65 用例:40 ✅ + 8 ⚠️ + 3 ❌ + 14 skip(几乎全在 G/H 文件上传)**

**Cowork 162 套件:148 ✅ + 6 ❌ + 8 skip**

---

## 🎉 v25-5 → v25-6 已修复

| Bug | v25-5 现象 | v25-6 验证 |
|---|---|---|
| **#1** A-3 `runCoworkSuite` 不存在 | 全局未定义 | ✅ 现在是 function;162 套件可跑(148/162) |
| **#2** E-3 完整纪要 docx 500 | `/api/meetings/{id}/minutes` 500 | ✅ 200,37897 byte,PK ZIP 头 |
| **#3** F-1 `/me` 服务器错误 + 缺 tab | 红色 toast + 没领导指令/上级文件 | ✅ 无 toast,新增 `领导指令`、`上级文件` tab |
| **W-2** C-8 leader 进 /admin/team 弹 403 | 数据渲染 + 红色 toast | ✅ 数据渲染,无 403 toast(允许 leader 角色) |
| **W-4** E-2 实录无 [mm:ss] | UI 吃了时间戳 | ✅ `[00:00]` `[00:06]` `[00:12]` `[00:18]` 全部显示 |

---

## ❌ 仍未修复(原 bug)

### Bug #4 — F-4 · directive commit 仍然不派发【P0】

**现象**:这条 v25-5 报过,**v25-6 完全不变**

```js
// leader 登录
fetch("/api/me/directives", {method:"POST", credentials:"include",
  headers:{"Content-Type":"application/json"},
  body: JSON.stringify({content: "请房屋安全科本周完成 3 栋 D 级危房应急避险措施"})})
.then(r=>r.json()).then(d => console.log(d))
// 返回 directive id  ✅

// commit 一个 task 给冯磊
fetch(`/api/me/directives/{id}/commit`, {method:"POST", credentials:"include",
  headers:{"Content-Type":"application/json"},
  body: JSON.stringify({tasks: [{
    content: "完成3栋D级危房应急避险措施",
    assignee_user_id: "1ecc73c6-0110-4b0b-937c-e87df9bfe04f",
    due_at: "2026-05-18T..."}]})})
.then(r=>r.json())
// 返回:
// {
//   "directive_id": "d65c14bc-…",
//   "committed_task_ids": ["d405b282-…"],
//   "dispatched_count": 0    ← ❌ 应该 = 1
// }

// 用 committed_task_ids 取任务 → 都 404
fetch("/api/task/d405b282-…")  // 404
fetch("/api/tasks/d405b282-…") // 404
fetch("/api/me/tasks?status=all") // 列表里也找不到 d405b282
```

**期望**
- `dispatched_count: 1`
- `GET /api/task/{id}` 200,status = `dispatched`
- `/api/me/tasks?status=all` 含此 task

**怀疑位置**:`commit` handler 写 task 但缺 `dispatched_at` / 把任务写到了游离表 / 没 commit transaction。**这个 bug 直接断 F-4 → F-5 → F-6 → F-7 → F-8 整条任务闭环 demo**

---

### Bug #5 — F-9 · 4 维自动派发端点仍全 404【P2】

仍然全部 404,与 v25-5 一致:

```
POST /api/me/directives/{id}/recommend       → 404
POST /api/me/directives/{id}/auto-dispatch   → 404
POST /api/dispatch/recommend                  → 404
POST /api/dispatch-recommend                  → 404
```

要么端点路径需要写回测试用例,要么功能未实装 — 二选一,确认下。

---

## ❌ v25-6 新发现

### Bug #6 — L-2 · ABAC 漏洞:expert 能进 admin 页【P1 安全】

**现象**:用 expert 账号 `demo.fengl@futian.gov.cn`(物业 expert,绑定 物业监管 AI)登录后:

1. 直接访问 `https://aimeeting.zhzjpt.cn/admin/team`
   - 页面**完整渲染** 24 个成员,包括 owner 邮箱、admin 部门绑定、其它 expert 绑定的 AI
   - 右下角弹一个 `请求失败 (403) owner / admin / leader required` toast(只是某个 admin-only 接口被拒,主数据没拒)
   - 页面 **没有**重定向

2. 直接访问 `https://aimeeting.zhzjpt.cn/admin/agents`
   - 页面**完整渲染** 16 个 AI 全部 persona / 关键词 / KB 绑定,带「编辑 / 删除」按钮
   - 没有 403 toast,没有重定向

**API 验证(用 expert cookie)**
```js
fetch("/api/users", {credentials:"include"})        // 200 ❌(应 403)
fetch("/api/team/members", {credentials:"include"}) // 200 ❌(应 403)
fetch("/api/agents", {credentials:"include"})        // 200 ❌(应 403,或仅返回与该 expert 绑定的)
```

**期望**
- expert 进 `/admin/*` 路由整页 redirect 到 `/` 或显示 「无权限」
- `/api/users`、`/api/team/members`、`/api/agents` 对 expert 应:要么 403,要么仅返回与之相关的子集

**修复建议**:在 admin 页路由 layout 加角色 guard;后端 `/api/users` 等接口加 ABAC check

---

## ⚠️ 警告 / Partial

| 标号 | 现象 | 影响 |
|---|---|---|
| **W-1** A-2 | seed 数 `transcripts: 32` (<spec 50)、`kb_chunks: 48` (<spec 80) | 与 v25-5 同 — 看是改 seed 还是改 spec |
| **W-7 (新)** A-3 | 162 套件:148 pass / 6 fail / 8 skip;spec 要 0 fail。失败的:`K-6` `X-1` `X-6` `X-21` `X-25` `YY-1` (前 5 个 "no moderator agent" / "no_signal_or_no_agenda";YY-1 期望 400 实际 422) | 套件可跑了,但用例本身有 6 个 regress |
| **W-3** D-4 | 物业监管 KB 文档 `chars` 191/192/214(spec >200) | 同 v25-5 — seed 文档加几句话即可 |
| **W-8 (新)** C-2 | `总任务: 74`(seed 只创建 30,余下来自 cowork 套件后遗) | 跑完 cowork 应该自动清,或 dashboard 加 demo-only filter |
| **W-9 (新)** C-6 | `/dashboard/kanban-agents` 16 列全部「暂无任务」 | 任务和 AI 的关联在 kanban 没体现,可能 task.agent_id 字段没 seed |
| **W-10** I-3 | 跨账户访问申请创建后,leader 在 `/admin/access-requests` 看不到(因 target_owner 是 owner 账号而非 leader) | 是测试假设问题不是 bug,但用例验收路径要更明确 |
| **W-11** M-2 | 错误账号登录提示 `incorrect email or password`(英文) | 应中文 `邮箱或密码错误` |
| **W-12** M-3 | 缺 confirm POST → 422(spec 要 400) | FastAPI 默认 422 (Unprocessable Entity);若一定要 400,后端要前置校验 |

---

## ✅ 全部通过的用例(快速过)

**A-1** wipe 231 行;**A-2** seed 16 agents/19 users skipped/10 meetings/30 tasks/16 KBs/48 docs/embed_failed=0
**B-1** 审计 2 条 wipe+seed;**B-2** healthz `{ok,env:prod,sentry_active:false}`;**B-3** quota `30/200/60s`;**B-4** 9 个 admin tab;**B-5** 操作日志 167+ 条降序
**C-1** leader 登录 `李建国`;**C-2** 4 KPI + 4 图;**C-3** task_by_status 模板出图;**C-4** 趋势 3 卡;**C-5** 日清 xlsx 5973 字节 PK;**C-7** 用户 Kanban 6+ 列;**C-8** 24 成员无 403 toast
**D-1** 16 agents;**D-2** 物业监管关键词全中;**D-3** 16 KBs;**D-5** 公文审核 4 条问题(严重×2 + 一般 + 建议)
**E-1** 10 会议;**E-2** 实录带 `[mm:ss]`;**E-3** 完整纪要 docx 37897 byte;**E-4** 简版 md/docx;**E-5** ongoing 状态正确;**E-6** 议程 3 项匹配 spec
**F-1** /me 无错误 toast,tabs 含领导指令/上级文件;**F-2** 任务详情结构 OK;**F-3** LLM 拆解 1 draft 命中房屋安全科;**F-10** 404 (spec 允许)
**G** 上级文件 tab 入口可用,空态文案 OK
**H-4** KB 文档删除 `/api/knowledge-bases/{kb}/documents/{doc}` 204
**I-1** 访问申请空态;**I-2** POST 创建 access-request 200;**I-4** approve 路径存在
**J-1** 通知 drawer 含 4 条;**J-2** alerts force-check 3 维返回;**J-3** monthly-eval 23 users / 6 workspaces
**K-1** quota 自增 1;**K-2** firstReject = 30 ∈ [1,31]
**L-3** 演示脚本 docs/客户演示脚本.md 11381 byte
**M-1** 未登录 → /login?next=...;**M-4** 错 confirm "no" → 400;**M-5** 错 task ID 显示 "task not found"

---

## A-3 Cowork 162 套件失败明细

```json
[
  {"id":"K-6",  "error":"no moderator agent"},
  {"id":"X-1",  "error":"no moderator agent"},
  {"id":"X-6",  "error":"unexpected: {\"fired\":false,\"payload\":null,\"note\":\"no_signal_or_no_agenda\"}"},
  {"id":"X-21", "error":"not fired: ... \"note\":\"no_signal_or_no_agenda\""},
  {"id":"X-25", "error":"not fired: ... \"note\":\"no_signal_or_no_named_speakers\""},
  {"id":"YY-1", "error":"期望 400, 实际 422"}
]
```

- K-6 / X-1:依赖一个 「moderator agent」,seed 没建?
- X-6 / X-21:依赖 agenda 有信号才 fire alert
- X-25:依赖 named speaker 信号
- YY-1:状态码 spec 不一致(同 W-12)

---

## 修复优先级

| P | 问题 | 预估影响 |
|---|---|---|
| **P0** | F-4 commit 不派发 / 任务找不到 | 直接断 F-4~F-8 整条 demo |
| **P1** | L-2 ABAC 漏洞:expert 能看 /admin/team 和 /admin/agents | 安全风险 |
| **P2** | F-9 4 维派发端点 404 | 单功能,看产品决定路径或实装 |
| **P3** | A-3 套件 6 fails(K-6/X-1/X-6/X-21/X-25/YY-1) | 自动化基线 < 152 阈值 |
| **P4** | seed 数据(transcripts/kb_chunks/chars)、文案中文化、422→400 | 体验细节 |

---

## 给 Claude Code 的修复优先级一句话

> v25-6 修了 5 条致命 bug(完整纪要、runCoworkSuite、/me 错误、time stamp、leader 403 toast),非常好 🎉。还剩 3 条要修:
> 1. **P0** `POST /api/me/directives/{id}/commit` 写 task 后 `dispatched_count` 永远 0,而且返回的 task_id 在所有 GET 接口都 404 — 怀疑事务没 commit 或者写到了 detached 表。这个直接断领导指令派发整条 demo。
> 2. **P1** expert 角色能 GET 全量 `/api/users`、`/api/team/members`、`/api/agents`,以及直接访问 `/admin/team`、`/admin/agents` 页面看到完整数据 — ABAC 中间件需要补 expert/member 角色拦截。
> 3. **P2** 4 维自动派发端点(`/api/me/directives/{id}/recommend`、`/auto-dispatch` 等)全 404,不知道是改名了还是没实装,要么实装要么写回 spec。
