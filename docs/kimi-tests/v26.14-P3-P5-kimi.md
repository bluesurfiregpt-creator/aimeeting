# v26.14 P3 + P5 Kimi 用例 · AI 履历 + 议程 主动 推进 + 全景 时间线

> 版本: v26.14-P3 + P5.1 + P5.2 + P5.3 + P5.4 合 测
> 范围:
>   **P3 (AI 详情页 履历)**:
>     - GET /api/agents/{id}/activity 新 endpoint (total_lines / total_meetings / recent_meetings)
>     - hero 加 3 stat chip (📣 召唤 N / 💬 发言 N / 🎙️ 参与 N)
>     - 加 📋 履历 (N) tab — 列 最近 8 场, 含 status chip + 直跳 会议
>   **P5.1 (后端 议程 进度 + dev/inject)**:
>     - Meeting Model: current_agenda_idx + agenda_progress (init_db.py 自动 migrate)
>     - GET /api/meetings/{id}/agenda-progress (lazy init on ongoing)
>     - POST /api/meetings/{id}/agenda-advance (30s rate-limit, ABAC: leader+ OR 创建人)
>     - POST /api/meetings/{id}/agenda-jump (无 rate-limit)
>     - WS event "agenda_advanced" 全房间 同步
>     - **POST /api/meetings/{id}/dev/inject-monitor-event** (owner only) — Kimi 测 P4 三档 UI 用
>   **P5.2 (前端 议程 进度条 + 推进)**:
>     - 老 read-only strip → 进度式: ● active / ✓ done / ○ pending
>     - 当前 active 显 实/预 时间 (12/15m), 80% 琥珀, 100% 红
>     - controller 见 [推进 →] 按钮, 末项 [✓ 完成 末项]
>     - 自 tick 30s — elapsed 自走 不依赖 后端 polling
>   **P5.3 (LLM 推进 建议)**:
>     - agenda_monitor _SYSTEM_PROMPT 加 should_advance + advance_reason
>     - 优先级: stuck > off_topic(强) > advance_suggested > time_warning > off_topic(弱)
>     - 顶部 banner emerald 主色 (🚀) + "立刻 推进 →" CTA (controller 见)
>   **P5.4 (全景 时间线)**:
>     - GET /api/meetings/{id}/timeline — agenda_progress + audit_log 合
>     - phase=ended 时 minutes tab 顶部 (在 HarvestPanel 下方) 渲 MeetingTimelinePanel
>     - 7 种 kind 不同 颜色 (rose/amber/emerald/violet/灰)
> 测试者: Kimi
> 预计 时长: 30-40 分钟

---

## 顶部 6 条 死规矩 (反幻觉)

1. **真实页面 / HTTP** — 每步 Pass/Fail 基于 浏览器 实际 看到 的 DOM 或 curl 输出
2. **每步 独立 判定** — 不允许 "按理说应该..." / "上一步过了 这一步也应该过"
3. **复述 原文** — 引用 必须 带 引号 + 是 实际 渲染 出来 的 字面字符
4. **不许 跳步** — fail 则 后续 BLOCKED
5. **不许 编造账号** — 仅 §账号 表 5 个 账号
6. **每用例 4 段** — (1) 实际 看到 (2) 判定 pass/fail/blocked (3) 失败 理由 (4) 证据 (截图 + JSON 原文)

---

## 环境 与 账号

**入口**: `https://aimeeting.zhzjpt.cn`

| 角色 | 邮箱 | 密码 |
|------|------|------|
| owner | `bluesurfiregpt@gmail.com` | `aimeeting123` |
| leader | `demo.lijg@futian.gov.cn` | `demo123` |
| admin | `demo.chensy@futian.gov.cn` | `demo123` |
| expert | `demo.fengl@futian.gov.cn` | `demo123` |
| member | `demo.hanx@futian.gov.cn` | `demo123` |

**前置 假设**:
- workspace 至少 1 个 AI agent (任何 是 active 的) — T-01 ~ T-03 用. 用 `<AID>` 代 ID.
- workspace 至少 1 个 **进行中 (ongoing)** 会议 + agenda 有 ≥ 2 项 — T-04 ~ T-09 用. 用 `<MID>` 代 ID.
- workspace 至少 1 个 **已结束 + 议程 走过** 会议 — T-10 用. 用 `<MID-end>` 代 ID.

---

## 预检 P-1 ~ P-5

### P-1 · 生产 健康
```bash
curl -sS -o /dev/null -w "%{http_code}\n" https://aimeeting.zhzjpt.cn
```
**Pass**: `200`. **Fail**: 后续 全 BLOCKED.

### P-2 · 新 endpoint 路由 注册
```bash
# 都 应 401 (路由 活, 仅 没登录)
curl -sS -o /dev/null -w "agent activity: %{http_code}\n" https://aimeeting.zhzjpt.cn/api/agents/00000000-0000-0000-0000-000000000000/activity
curl -sS -o /dev/null -w "agenda progress: %{http_code}\n" https://aimeeting.zhzjpt.cn/api/meetings/00000000-0000-0000-0000-000000000000/agenda-progress
curl -sS -o /dev/null -w "agenda advance: %{http_code}\n" -X POST https://aimeeting.zhzjpt.cn/api/meetings/00000000-0000-0000-0000-000000000000/agenda-advance
curl -sS -o /dev/null -w "dev inject: %{http_code}\n" -X POST -H "content-type: application/json" -d '{"event_type":"x"}' https://aimeeting.zhzjpt.cn/api/meetings/00000000-0000-0000-0000-000000000000/dev/inject-monitor-event
curl -sS -o /dev/null -w "timeline: %{http_code}\n" https://aimeeting.zhzjpt.cn/api/meetings/00000000-0000-0000-0000-000000000000/timeline
```
**Pass**: 全部 `401`. **Fail**: 任一 `404` → 对应 endpoint 没 deploy → BLOCKED 对应 用例.

### P-3 · owner 登录 + cookie 拿 出
浏览器 `/login` → owner 账号 → 跳 `/`. dev tools → cookie `aimeeting_session=...` 抄 出, 后续 curl 用.
**Pass**: cookie 有 + 主页 显 大标题. **Fail**: BLOCKED.

### P-4 · 找 一个 进行中 + 有 agenda 的 会议 ID
浏览器 `/me/meetings` 列表 → 找 status=进行中 + 议程 ≥ 2 项的 → 进 会议室 → URL 抄 `<MID>`.
若 没有, 需 owner 新建 一个 (mode=hybrid + 设 ≥ 2 个 议程项, 例 [{"title":"主题A","time_budget_min":2}, {"title":"主题B","time_budget_min":3}]) + 点 开始会议 → 拿 `<MID>`.
**Pass**: `<MID>` 拿到. **Fail**: SKIP T-04~T-09.

### P-5 · 找 一个 已结束 会议 ID (有 议程 + 走过)
列表 找 status=已结束/已沉淀 → URL 抄 `<MID-end>`.
**Pass**: `<MID-end>` 拿到. **Fail**: SKIP T-10.

---

# 🟦 P3 用例 (T-01 ~ T-03)

## T-01 · agent activity API (聚合 数据 正确) ★

**前置**: P-3 pass + workspace 有 ≥ 1 个 AI 已 在 至少 1 场 会议 中 发言过 (用 `<AID>`).

**步骤**:
```bash
COOKIE='aimeeting_session=...'
curl -sS "https://aimeeting.zhzjpt.cn/api/agents/<AID>/activity" -H "Cookie: $COOKIE" | jq
```

**Pass**:
- (a) HTTP 200
- (b) JSON 含 字段 `total_lines` (number ≥ 0) + `total_meetings` (number ≥ 0) + `recent_meetings` (array)
- (c) recent_meetings 中 每 项 含 `meeting_id`, `title`, `status`, `started_at`, `lines_by_agent`
- (d) total_meetings === recent_meetings.length 当 < 8 时; total_meetings ≥ 8 当 array len = 8 时

**Fail 情景**:
- 字段 缺失 / 类型 错 → backend 没 deploy
- HTTP 500 → 拉 logs 查 SQL 报错

**复述**: 完整 JSON response (不许 省略 字段).

---

## T-02 · 详情页 hero 3 stat chip ★

**前置**: T-01 pass.

**步骤**: 浏览器 进 `/me/profile/agents/<AID>`, 看 hero 右侧 dl 下方.

**Pass** (字面 / 顺序 一致):
- (a) 第 1 chip 字面 = `"📣 召唤 N 次"` (N 是 数字)
- (b) 第 2 chip 字面 = `"💬 发言 N 行"` (N 是 数字, 跟 T-01 total_lines 一致)
- (c) 第 3 chip 字面 = `"🎙️ 参与 N 场"` (N 是 数字, 跟 T-01 total_meetings 一致)
- (d) 三 chip 颜色 分别 violet / sky / emerald (border + text)

**Fail 情景**:
- chip 不显 → 前端 没 deploy
- 数字 跟 API 不一致 → 拉数据 路径 出错

**复述**: 三 chip 完整 字面 (一字 不差).

---

## T-03 · 履历 tab + 跳 会议室 ★

**前置**: T-02 pass.

**步骤**:
1. 同页 看 tab 栏, 应有 4 个 tab: 工卡 / 知识 (N) / 记忆 (N) / 履历 (N)
2. 点 `📋 履历 (N)` tab
3. 看 列出 的 最近 会议

**Pass**:
- (a) 第 4 个 tab 字面 含 `"📋 履历"` + 数字 N (= total_meetings, 跟 T-01 一致)
- (b) 点 后 列 出 ≥ 1 个 会议 行 (or 显 "这 AI 还没 参与 过 任何 会议" 空态)
- (c) 每 行 含: 会议 title + 状态 chip ("进行中" / "已结束" / "已沉淀") + 发言 行数 + 时间 + `→ 进入` 按钮
- (d) 点 `→ 进入` 跳 到 `/meeting/<id>` (URL 变)

**Fail 情景**:
- 第 4 tab 不存在 → 前端 没 deploy
- 跳转 失败 → Link 配置 错

**复述**: 截 履历 tab 截图 + 第 1 行 完整 字面.

---

# 🟦 P5 用例 (T-04 ~ T-10)

## T-04 · agenda-progress lazy init + 字段 完整 ★

**前置**: P-3 + P-4 pass.

**步骤**:
```bash
curl -sS "https://aimeeting.zhzjpt.cn/api/meetings/<MID>/agenda-progress" -H "Cookie: $COOKIE" | jq
```

**Pass**:
- (a) HTTP 200
- (b) JSON 含 字段: `current_idx` (int or null) + `total_items` (int) + `is_complete` (bool) + `has_agenda` (bool=true) + `items` (array)
- (c) 若 会议 ongoing + 第一次 调 — current_idx === 0 (lazy init), items[0].status === "active"
- (d) items 长度 === total_items === agenda 项 数
- (e) 每 item 含 `idx, title, status (active/done/pending), started_at, ended_at, elapsed_seconds`

**Fail 情景**:
- has_agenda=false → 会议 没 设 议程 (P-4 选 错), 重 选 一个
- current_idx=null + has_agenda=true → lazy init 没 跑 (status 可能 不是 ongoing)

**复述**: 完整 JSON.

---

## T-05 · 议程 strip 升级 (顶 chrome 下方 进度式) ★

**前置**: T-04 pass.

**步骤**: owner 浏览器 进 `/meeting/<MID>`, 看 顶部 chrome 下方 strip.

**Pass**:
- (a) 存在 `data-testid="agenda-strip"` 元素
- (b) Strip 内 显 "本场议程" 标题
- (c) 各 议程项 inline (横排), 第一项 前 显 `●` (绿) + title 加粗 白; 其他项 前 显 `○` 灰 + title 灰
- (d) 第一项 后 应 显 `(0/2m)` 或 类似 (实/预 时间, m 单位)
- (e) 项 之间 有 `→` 连接
- (f) Strip 末尾 owner (controller) 见 `[推进 →]` emerald 按钮 (`data-testid="agenda-advance-btn"`)

**Fail 情景**:
- 还显 老 read-only `<ol>` 编号 列表 → 前端 没 deploy
- 推进 按钮 不显 → ABAC 判错 (owner 应 是 controller)

**复述**: 截 strip 截图 + DOM `outerHTML` 前 500 字.

---

## T-06 · 推进 议程 (advance) + WS 同步 ★

**前置**: T-05 pass.

**步骤**:
1. owner 在 会议室, 等 30s+ (避开 rate-limit)
2. 点 `[推进 →]` 按钮
3. 看 strip 状态 + 看 toast 是否 出现

**Pass**:
- (a) 老 第一项 dot 变 `✓` 灰 + title 加 line-through; 第二项 dot 变 `●` 绿 + title 加粗
- (b) toast 出现 含 `"议程 推进 到 第 2 项"` 文字
- (c) curl GET agenda-progress 验: current_idx === 1, items[0].status === "done", items[1].status === "active"

**Fail 情景**:
- 点 后 没反应 → JS 报错, 看 dev console
- HTTP 429 → rate-limit 没 等够; 等 30s+ 重测
- HTTP 403 → ABAC 错误, owner 应 可推进

**复述**: 截 advance 前后 strip 截图 (2 张) + toast 字面.

---

## T-07 · 30s rate-limit 拦截 ★

**前置**: T-06 pass + 刚 完成 一次 advance (即 当前 active 项 started_at < 30s ago).

**步骤**: 立刻 再点 `[推进 →]` 按钮 (期望 拒)

**Pass**:
- (a) 收 HTTP 429
- (b) toast 含 类似 `"刚 进入 这项 不到 30 秒, 稍后 再推进"` 字面 (或 由 api.ts 默认 错误 toast)

**Fail 情景**:
- 直接 推进 成功 → rate-limit 失效
- 没 toast → 前端 错误 处理 缺失

**复述**: HTTP code + toast 字面 (or dev tool network 抓的 response body).

---

## T-08 · dev/inject 测 三档 off_topic UI (补 P4 SKIP 用例) ★★

**前置**: P-3 + P-4 pass + owner 在 `/meeting/<MID>` (WS 连上, phase=live).

> 这 是 v26.14-P5.1 给 Kimi 加 的 测试 工具. 老 P4 测 三档 UI 因 LLM 不易 触发 SKIP 了.

**步骤** — 三 子用例 各 独立:

### T-08a · severity=suspected (轻 toast)
另 开 terminal:
```bash
curl -sS -X POST "https://aimeeting.zhzjpt.cn/api/meetings/<MID>/dev/inject-monitor-event" \
  -H "Cookie: $COOKIE" -H "content-type: application/json" \
  -d '{"event_type":"agenda_off_topic","off_topic_severity":"suspected","off_topic_summary":"[T08a] 测 轻提示"}'
```
浏览器 看 顶部 chrome 下方 是否 出现 banner.

**Pass**:
- (a) HTTP 200 + JSON `{"ok":true,"injected":...}`
- (b) 渲 `data-testid="moderator-banner-off_topic-suspected"` (灰色 细窄)
- (c) 含 字面 `"可能 偏题:"`
- (d) ~12s 后 自动 消失

### T-08b · severity=confirmed (中 banner)
```bash
curl -sS -X POST "https://aimeeting.zhzjpt.cn/api/meetings/<MID>/dev/inject-monitor-event" \
  -H "Cookie: $COOKIE" -H "content-type: application/json" \
  -d '{"event_type":"agenda_off_topic","off_topic_severity":"confirmed","off_topic_summary":"[T08b] 测 确认 偏题","current_agenda_item":"测试项A"}'
```

**Pass**:
- (a) 渲 `data-testid="moderator-banner-off_topic"` (amber)
- (b) 大 召唤 按钮 字面 含 `"召唤"`

### T-08c · severity=severe (全屏 modal)
```bash
curl -sS -X POST "https://aimeeting.zhzjpt.cn/api/meetings/<MID>/dev/inject-monitor-event" \
  -H "Cookie: $COOKIE" -H "content-type: application/json" \
  -d '{"event_type":"agenda_off_topic","off_topic_severity":"severe","off_topic_summary":"[T08c] 测 严重","auto_summon_after_s":8}'
```

**Pass**:
- (a) 渲 `data-testid="moderator-modal-severe"` 全屏 modal + 模糊背景
- (b) 含 字面 `"🚨"` + `"严重 偏题"`
- (c) 右上 显 `"8s 后 自动 召唤"` 倒计时 chip
- (d) 可 点 `"忽略"` 取消 自动 召唤

**Fail 情景** (任一 子用例):
- 没渲 banner / modal → WS event 没收到 (重 连 试 一次)
- testid 错位 → severity 分支 改错

**复述**: 三档 截图 各 一张 + DOM testid 命中 截图.

---

## T-09 · 推进 建议 banner (T-08 同套 dev/inject) ★

**前置**: T-08 pass.

**步骤**:
```bash
curl -sS -X POST "https://aimeeting.zhzjpt.cn/api/meetings/<MID>/dev/inject-monitor-event" \
  -H "Cookie: $COOKIE" -H "content-type: application/json" \
  -d '{"event_type":"agenda_advance_suggested","advance_reason":"[T09] 测试 建议 推进","current_agenda_item":"主题A","next_agenda_item":"主题B"}'
```
浏览器 看 banner.

**Pass**:
- (a) 渲 `data-testid="moderator-banner-advance_suggested"` (emerald 主色)
- (b) 含 字面 `"🚀"` + `"建议 推进 → 「主题B」"`
- (c) 含 字面 `"[T09] 测试 建议 推进"` (advance_reason)
- (d) owner (controller) 见 emerald 按钮 `"立刻 推进 →"` (`data-testid="advance-suggested-accept"`)
- (e) 点 该 按钮 → 调 advance API → 议程 strip 应 同步 推进 (验 T-06 同样 行为)

**Fail 情景**:
- 不渲 → P5.3 前端 没 deploy
- 看 不到 emerald 按钮 → ABAC 错

**复述**: 截 banner 截图 + 点击后 strip 状态 截图.

---

## T-10 · 全景 时间线 (minutes tab) ★

**前置**: P-5 pass (有 一个 已结束 + 走过 议程 的 会议 `<MID-end>`).

**步骤**:
1. 浏览器 进 `/meeting/<MID-end>` (已结束 会议)
2. 应 自动 切 到 minutes tab
3. 看 顶部 (在 HarvestPanel 下方) 是否 有 时间线 panel

**Pass**:
- (a) 存在 `data-testid="meeting-timeline"` 元素 (若 该 会议 没 任何 议程 推进 + 没 audit 事件 则 不 渲, 算 SKIP 此用例)
- (b) panel 标题 含 `"🕒 全景 时间线"` + 数字 chip "N 条"
- (c) 列 出 ≥ 1 行, 每 行 含: 时间戳 (HH:MM:SS mono font 字号 小) + label
- (d) label 形如 `"▶️ 进入 议程 1: ..."` / `"✓ 完成 议程 N: ... (用时 Mm/Pm 预算)"` / `"⚠️ 严重 偏题 — ..."` 等
- (e) 各 行 左侧 border 颜色 不同 (rose/amber/emerald/violet/灰)
- (f) > 8 行 时 默认 折叠, 显 "展开 全部 (N)" 切换 链

**Fail 情景**:
- panel 不渲 + 该 会议 确实 走过 议程 → P5.4 没 deploy 或 audit 写入 缺失
- 字面 不 一致 → label 格式 改错

**复述**: 截 timeline panel 截图 + 列 前 3 行 完整 字面.

---

# 报告 模板

## 总结
- **总用例**: 12 (T-01 ~ T-10, T-08 含 a/b/c)
- **Pass**: ___
- **Fail**: ___
- **Blocked**: ___
- **结论**: 🟢 GREEN / 🟡 YELLOW (≤2 fail/blocked) / 🔴 RED (>2 fail)

## 用例 明细

(每 用例 4 段 报告 — 实际 看到 / 判定 / 失败 理由 / 证据)

## 反幻觉 自检

- [ ] 没截图 的 步 没 声称 "看到"
- [ ] 没把 `phase: "live"` 写成 `phase = 进行中`
- [ ] 没出现 `应该 / 通常 / 估计 / 似乎`
- [ ] JSON 复述 没 省略 字段
- [ ] 字面 字符 一字 不差 (留意 中文 空格 / emoji / 大小写)

---

# 已知 不 在 本次 范围

- 议程 中途 编辑 (live 中 改 议程 未 测 — 老 行为 应 是 锁住, 没 改 此次)
- agenda_advance_suggested LLM 真触发 (T-09 仅 走 dev/inject, 真 LLM 触发 待 后续 真实 会议 验)
- 多人 同时 advance 竞态 (没 测)
- timeline 排序 ts 同 时刻 的 顺序 (没 测)
