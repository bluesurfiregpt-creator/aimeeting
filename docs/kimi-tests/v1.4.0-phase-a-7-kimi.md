# Kimi 测试用例 · v1.4.0 Phase A · 7 — 死码清理 + 声纹周期 微调

> 部署: 2026-05-27 · commit `a53cd17` · 分支 `main`
> 范围: NORTH_STAR § 6.1 Phase A · 7 (~0.5d). Phase A 收官.

## 顶部 6 条死规矩 (反幻觉)
(同前)

## 环境与账号
- 入口: <https://aimeeting.zhzjpt.cn>
- 后端: 同 v1.4.0-phase-a-1-kimi.md
- 账号: `demo.lijg@futian.gov.cn` / `demo123`

---

## 预检 P-1 ~ P-3
(同 Phase A · 6 测试用例)

---

## 测试用例 T-01 ~ T-03

### T-01 · `closure_curator.py` 已删 (静态)

- **前置**: P-* pass
- **目标**: 校验 backend 容器 内 该文件 不存在.
- **步骤** (需 server SSH 访问 — PM 代跑, Kimi 沙箱不可):
  ```bash
  ssh root@aimeeting "ls /opt/aimeeting/backend/app/closure_curator.py 2>&1"
  ```
- **Pass**: `No such file or directory` (返回 1)
- **Fail**: 文件 仍 存在 → deploy 没 删 文件 (rsync --delete 应 handle, 验证)
- **必须复述**: ls 输出 完整

### T-02 · `PERIODIC_INTERVAL_S = 15` 字面 in container

- **前置**: T-01 pass
- **目标**: 声纹 识别 周期 已 改.
- **步骤** (需 server SSH):
  ```bash
  ssh root@aimeeting "cd /opt/aimeeting/deploy && docker compose exec backend grep 'PERIODIC_INTERVAL_S' app/identify_pipeline.py | head -3"
  ```
- **Pass**: 输出 包含 字面 `PERIODIC_INTERVAL_S = 15`
- **Fail**: 输出 仍 `PERIODIC_INTERVAL_S = 45` → Docker layer cache 没 invalidate, 需 --no-cache 重 build
- **必须复述**: grep 输出 原文

### T-03 · MOCK_HUMANS TD 注释 已加入 frontend bundle (静态)

- **前置**: P-* pass
- **目标**: 校验 audit 注释 deploy 到 frontend (确保 后续开发者 不 误以为 mock 是 真接).
- **步骤**:
  ```bash
  for c in $(curl -sS https://aimeeting.zhzjpt.cn/m/meetings/probe | grep -oE '/_next/static/chunks/[^"]+\.js' | sort -u); do
    hit=$(curl -sS "https://aimeeting.zhzjpt.cn$c" 2>/dev/null | grep -c "MOCK_HUMANS")
    if [ "$hit" -gt 0 ]; then echo "$c -> $hit"; fi
  done
  ```
- **Pass 条件 A**: 命中 ≥ 1 chunk (MOCK_HUMANS 仍 在 bundle, 配 TD 注释)
- **Pass 条件 B**: 0 命中 — 也合理 (Tree-shake 后 ID 重命名, MOCK_HUMANS 字符 没了)

实际 校验 应 看 production source map 或 直接 看 `shared/avatars.tsx` 源码,
sandbox 跑不动. Kimi **改判**: 直接 复述 commit `a53cd17` 里 `shared/avatars.tsx`
的 diff (PM 提供), 看 TD 注释 字面 `"⚠️ v1.4.0 Phase A · 7"`.

- **Fail**: TD 注释 不在 diff 中

---

## 报告模板

```
【总结】
P-* + T-01 ~ T-03: ...
结论: <GREEN / YELLOW / RED>

【用例明细】
...
```

---

## 反幻觉自检

- [ ] T-01 T-02 需 SSH 访问 — 没有 access 的 case 改为 SKIP, 不允许 编造 结果
- [ ] T-03 改判 / 改用 diff 校验 — 必须 复述 字面 字符串

---

## 已知不在 本次 范围

- **MOCK_HUMANS 真删**: 1-2d 工作, 留 Phase B/C (NORTH_STAR § 6.2-6.3).
- **声纹 阈值 0.65 → 0.55**: PM 决策 暂不动 (v25.10 客户反馈 0.65 是 fix 后值).
  PM 收 真 5-10 场 hybrid 会议 后 数据驱动 评估.
- **Phase A 整体 整合 smoke**: PM 后置 — 开 1 场 真 hybrid 会议 走完 7 项 全 链路
  (mic → ASR → AI 主动 → 立场 → 接力 banner → summary 结构化 + 任务溯源).

---

## Phase A 整体 收官 PM 后置 checklist

完成 7 项 后, 建议 PM 跑 一遍 端到端:

1. **打开 web** /meeting/<id>/live (Phase A · 6) → 浏览器 弹 mic 权限 弹窗
2. **说**: "我们要 灰度 B 组 20%, 看看 数据 / 法务 / UX 怎么 想" (含 3 个 keyword)
3. **观察**:
   - Phase A · 2 阈值 4s 内 真 AI chime in
   - Phase A · 3 立场守门: AI 不和稀泥, 明确 选边
   - Phase A · 5 recommend banner: Mira 推荐 下一个 接力 (可 mobile 也开 同步 看)
4. **结束 会议** → /m/meetings/<id>/summary (Phase A · 4) 看 结构化 summary:
   - 按 topic 分组
   - 每 topic 显 真人 + AI 立场 pill
   - 任务溯源 chip → 点 跳 回 实录 高亮

整套 走通 = Phase A 真正 ship.

---

> 测试 commit: `a53cd17`
> 用例 路径: `docs/kimi-tests/v1.4.0-phase-a-7-kimi.md`
