# Kimi 测试用例 · v26.3-08 整场 45 分钟硬上限

> **变更内容**:`feat(v26.3-08)` 整场会议 running 累计 ≥ 45 min 触发软 COMPLETE + 前端显示已用时间
> **commit**:本批次合并多 commit(参见 git log)
> **部署地址**:`https://aimeeting.zhzjpt.cn`
> **测试对象**:Kimi(AI 自动化测试,非人工)
> **本次目标**:验证整场 45 分钟硬上限不会误触发也不会漏触发,paused 时间不算,前端显示已用 + 颜色三档

---

## 0. 给 Kimi 的 6 条死规矩(读完不忘)

1. 只看真实页面 / 真实 HTTP 响应.推测一律 fail.
2. 每一步判定基于本步实际数据,不复用上一步.
3. 复述要原文带引号.如 `phase: "done"`,`last_error: "meeting_timeout"`.
4. 顺序依赖.前置 fail 后续 BLOCKED.
5. 不允许编账号 / URL / 字段名.全是 字面常量.
6. 每个用例必须给 4 段:`实际看到 / 判定 / 失败理由 / 证据`.

---

## 1. 环境 + 账号

| 角色 | 邮箱 | 密码 |
|------|------|------|
| owner | `bluesurfiregpt@gmail.com` | `<SYSTEM_OWNER_PWD>` |

**入口**:`https://aimeeting.zhzjpt.cn`

**数据库前置假设**:workspace 内有 ≥4 个 bound primary_user_id 的 expert agents + 1 个 moderator agent(同 v26.3-05 用例).

---

## 2. 预检 P-1 ~ P-3(同 v26.3-05,跑一遍)

### P-1 `curl /` → 200
### P-2 `curl /api/agents` → 401
### P-3 登录 → 顶栏出现 owner 邮箱

(详细判定 + 复述要求 见 `v26.3-05-kimi.md` §2,此处略.)

任一 fail → 后续 T-* 全 BLOCKED.

---

## 3. 测试用例 T-01 ~ T-06

### T-01 · 新会议 → state 里有 `running_elapsed_sec` + `max_meeting_sec` 字段

**前置**:P-1/P-2/P-3 全 pass
**目标**:验证 v26.3-08 后端 schema 已经生效

**步骤**:
1. 在首页创建 auto 会议:议程 2 项(任意标题)、勾 3 个 expert、标题 `kimi-test-08-T01-<时间戳>`
2. 跳到 `/meeting/<id>/orchestrate` 后,**不要** 点启动
3. 浏览器 F12 → Network → 找最近一次 `GET /api/meetings/<id>/orchestrate/state`
4. 看 response body

**Pass 条件**(全部):
- response body 是 JSON,**包含字段** `running_elapsed_sec` (字面值是 0 或浮点数,number 类型)
- response body **包含字段** `max_meeting_sec` (字面等于 `2700`,即 45*60)
- response body **包含字段** `phase`(字面等于 `"idle"`)

**Fail 条件**:
- response 没有 `running_elapsed_sec` 字段 → fail("v26.3-08 后端 schema 未生效")
- `max_meeting_sec` 不是 2700 → fail(配置错或路由没重启)

**必须复述**:把完整 response JSON 粘贴到报告(只这一次,不要省略 任何字段)

---

### T-02 · idle 时前端 不显示 已用时间

**前置**:T-01 pass,还在 idle 控制台页
**步骤**:
1. 看页面右上角 phase 徽章 (`未启动 / idle`)旁边
2. 寻找 `⏱` 时间显示

**Pass 条件**:
- idle 状态 **没有** `⏱` 时间显示(因为还没启动)
- 只显示 phase 徽章 + `0 / 2 议程完成`

**Fail 条件**:
- idle 状态 看到 `⏱ 0:00 / 45:00` → fail("idle 不该显示 计时器")

**必须复述**:截图整个页面顶部右侧

---

### T-03 · 启动 → 前端开始显示已用时间

**前置**:T-02 pass
**步骤**:
1. 点 🚀 启动 AI 自主讨论
2. 等 8 秒
3. 截图整个页面顶部右侧

**Pass 条件**(全部):
- phase 徽章 变 `运行中 / running`
- 旁边出现 `⏱ 0:0X / 45:00` (X 是 1-9 数字)
- API 验证(浏览器 cookie):
  ```bash
  curl -sS https://aimeeting.zhzjpt.cn/api/meetings/<id>/orchestrate/state -b "<cookies>"
  ```
  - `running_elapsed_sec` 字段 字面 是浮点数,**大于 0.5 且小于 30**(刚启动 8 秒)
  - `phase` 字段 字面等于 `"running"`

**Fail 条件**:
- `⏱` 没出现 → fail("前端 timer 渲染失效")
- `running_elapsed_sec` ≥ 30 → fail("计时器异常快")
- 显示绿色 / 蓝色 而不是灰色 → fail("颜色 tone 异常,前 30 min 应是灰 zinc-400")

**必须复述**:
- 完整 `GET /orchestrate/state` JSON
- `⏱ X:XX / 45:00` 截图

---

### T-04 · 暂停 → 计时器 冻结 (Q8=B 决策落地)

> 修订 2026-05-13:第一版 T-04/T-05 用 UI 点暂停按钮,2 议程会议 92 秒跑完,
> Kimi 决策时间内 phase 已 done — 抓不到 running.改用 curl 直接调 pause
> endpoint (wall-clock < 200ms),并要求 创建 **多议程**(5 项)会议 拉长跑时.

**前置**:T-03 pass
**步骤**:
1. 创建 **新** auto 会议(不复用 T-01 的 2 议程会议),专门用于 T-04/T-05:
   - 标题:`kimi-test-08-T04-<时间戳>`
   - **议程 5 项**(标题随便,每项 8 字以上,例:`议程一二三四五六` 这种,让 LLM 多 讲点)
   - 勾 **4 个 expert**
   - 创建后记下 `<M4>` = 新会议 UUID
2. 跳 `/meeting/<M4>/orchestrate`,**用 curl 启动**(比点按钮快,但 UI 启动也行):
   ```bash
   curl -sS -X POST https://aimeeting.zhzjpt.cn/api/meetings/<M4>/orchestrate/start -b "<cookies>"
   ```
3. 等 **8 秒** 让 LLM 进入第一议程
4. 拉一次 state,记 `running_elapsed_sec` = X1
5. **立即用 curl pause**(关键:不要点 UI 按钮,wall-clock < 200ms):
   ```bash
   curl -sS -X POST https://aimeeting.zhzjpt.cn/api/meetings/<M4>/orchestrate/pause -b "<cookies>"
   ```
6. 看 pause 返回的 JSON 里 `phase` 字段
7. 等 **15 秒** 壁钟
8. 拉一次 state,记 `running_elapsed_sec` = X2

**Pass 条件**(全部):
- pause 调用返回 status 字面等于 `200`
- pause response JSON 中 `phase` 字面等于 `"paused"`
- pause response JSON 中 `paused_at` 字段 字面 非 null,是 ISO 字符串
- X2 - X1 字面值 **< 3 秒**(只允许 0-3 秒浮动,因 LLM 飞行间隙 + 拿到信号有微秒级延迟)
- **绝不允许 X2 - X1 > 5**(若超过 → fail,paused 期间还在累计 = Q8 决策失效)

**Fail 条件**:
- X2 - X1 > 5 秒 → fail("paused 期间继续累计,Q8 决策未生效")
- pause 调用后 phase 不是 `"paused"` → fail
- 调用 pause 前 phase 已是 `"done"` → fail("会议太短,需 5+ 议程,T-04 用例数据建错")

**必须复述**:
- pause 调用的 wall-clock UNIX timestamp(精确到秒)
- X1 数值 + X2 数值 + 差
- pause 调用的 response JSON 全文

---

### T-05 · 恢复 → 计时器从冻结值继续 (不归零)

**前置**:T-04 pass,会议 `<M4>` 现 phase=paused,计时器停在 X2
**步骤**:
1. **用 curl 恢复**:
   ```bash
   curl -sS -X POST https://aimeeting.zhzjpt.cn/api/meetings/<M4>/orchestrate/resume -b "<cookies>"
   ```
2. 看 resume 返回的 JSON 里 `phase`
3. 等 **6 秒** 壁钟
4. 拉 state,记 `running_elapsed_sec` = X3

**Pass 条件**:
- resume response 中 `phase` 字面等于 `"running"`
- X3 字面值 **> X2** 且 **< X2 + 12**(从冻结点继续,不归零,且累计 ≈ 6 秒;允许 0-6 秒额外余量)
- resume response 中 `paused_at` 字段 字面等于 `null`

**Fail 条件**:
- X3 < X2 → fail("时间回退,逻辑错")
- X3 > X2 + 20 → fail("把 paused 那 15 秒补上了,Q8 决策错误")
- X3 ≈ 0(差值 |X3 - X2| > X2 / 2 → fail,意味 归零)

**收尾**:
- 跑完 T-05 后,**用 curl cancel** 终止会议,免得 浪费 LLM:
  ```bash
  curl -sS -X POST https://aimeeting.zhzjpt.cn/api/meetings/<M4>/orchestrate/cancel -b "<cookies>"
  ```
  (不影响 T-04/T-05 判定)

**必须复述**:X2 / X3 / 差 + resume response JSON 全文

---

### T-06 · 终态 done 时计时器仍冻结 + 不显示 timeout banner(因正常完成)

**前置**:T-03 pass(用 T-01 创建的 2 议程会议 — 不是 T-04/T-05 创建的 5 议程会议)
**步骤**:
1. 回到 T-01 创建的会议 `/meeting/<id from T-01>/orchestrate`
2. 等会议跑到 phase=done(预计 1-3 分钟)
3. 看页面 顶部

**Pass 条件**(全部):
- phase 徽章 字面 `已结束 / done`
- `⏱` 仍显示一个 字面数 (非 0,非 "45:00"),字面数应小于 `5:00`(因议程少跑得快)
- **没有** "因 45 分钟整场硬上限提前 finalize" 这个 amber banner(因为是正常 COMPLETE,不是 TIMEOUT)
- API `running_elapsed_sec` 字段 字面 < 300

**Fail 条件**:
- 出现 timeout banner → fail("正常完成 误显示 timeout")
- `⏱` 数字归零 → fail("终态丢失累计")

**必须复述**:终态时 完整 state JSON + 截图

---

## 4. 边界检验(可选,如时间充裕跑一两个)

### B-01 · 老 auto 会议(v26.3-08 之前创建)兼容
- 找一个 历史 已完成 的 auto 会议(列表往下翻)
- 进 `/meeting/<id>/orchestrate`
- Pass 条件:不报 500,`running_elapsed_sec` 字面等于 `0.0`(老 state 没 running_started_at + paused_running_seconds 字段,helper 兼容返 0)

### B-02 · 颜色三档分界
模拟方式:无法直接造 30/40 min 数据.但可以读前端代码 `elapsedTone()`:
- ratio < 30/45 → `text-zinc-400`(灰)
- 30/45 ≤ ratio < 40/45 → `text-amber-300`(琥珀)
- ratio ≥ 40/45 → `text-rose-300`(红)

记录这个映射在你的报告里就行,无需真跑到这个时长.

---

## 5. 报告模板

```markdown
# v26.3-08 Kimi 测试报告

> 测试时间:<YYYY-MM-DD HH:MM:SS> (UTC+8)
> 入口:https://aimeeting.zhzjpt.cn
> 账号:bluesurfiregpt@gmail.com

## 总结
- pass / fail / blocked 计数
- 整体结论:GREEN / YELLOW / RED

## P-1, P-2, P-3 明细
(4 段格式)

## T-01 ... T-06 明细
(4 段格式,T-01/T-03/T-04/T-05/T-06 必须含 JSON 原文)

## 边界检验(可选)

## 异常 / 观察(可选)
```

---

## 6. 反幻觉自检(Kimi 提交前自查)

- 我有没有把 `running_elapsed_sec: 6.234` 写成 "elapsed 约 6 秒"? → 必须原文
- 有没有在没截图的步骤声称"看到"计时器? → 没有就 BLOCKED
- T-04 paused 那 15 秒,我有没有 真的等 15 秒(壁钟),而不是 估计? → 给 timestamp
- `phase` 字面值是否用引号: `"running"` 而不是 `running`? → 必须带引号
- 我有没有写"应该 / 通常 / 估计"? → 一旦出现 报告作废

---

## 7. 已知不在本次范围

- 真跑到 45 分钟 触发实际 timeout(成本太高,跳过.如想验证 timeout 路径,见 §4 B-01 + 后端日志查 `MAX_MEETING_SECONDS` 常量值即可)
- TIMEOUT 后 summary_md banner 文字(本批未实际触发,只验证代码路径在;真触发由 v26.3-09 cowork suite 模拟测)
- per-meeting `max_total_seconds` 字段(v26.3.1 才会做,本次是全局常量)

---

**END.**
