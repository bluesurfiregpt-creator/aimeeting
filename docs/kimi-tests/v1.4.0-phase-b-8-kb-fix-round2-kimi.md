# v1.4.0 Phase B · 8 NEW-C — Round 2 KB hits 验证 (commit 1dfb428)

> **背景**: Round 1 双盲 (Kimi + Claude GREEN 一致) 发现 `chat_kb_hits_total = 0`
> 是 backend 真问题. 根因: demo_seed_v2 只 创 agent 没配 KB → retrieve 直接空.
> 修复: 给 10 英文品牌 agent (Mira / Lex / Sage / ...) 灌 KB 文档 + 批量 embed.

## 0 · 反幻觉 6 条死规矩

1. 只看 真实 HTTP 响应 (SSE stream)
2. 每步 判定 基于本步 实际 看到的 metric 数字
3. 复述 用 原文 JSON (含 引号)
4. 不允许 跳步 (前一步 fail 则后续 BLOCKED)
5. 不允许 编造账号 / agent_id / metric
6. 每用例 4 段: 实际看到 / 判定 / 失败理由 / 证据

## 1 · 环境 与 账号

```bash
export REPO_ROOT=/sandbox/aimeeting   # Kimi sandbox 路径, 按实际改
```

### URL
- 生产: `https://aimeeting.zhzjpt.cn`
- Chat API: `https://aimeeting.zhzjpt.cn/api/agents/<agent_id>/chat`

### 唯一账号
| 邮箱 | 密码 | 用途 |
|------|------|------|
| `demo.lijg@futian.gov.cn` | `demo123` | leader, chat 调用方 |

### 数据库前置假设
- workspace 福田 demo 存在
- agent `Mira` (id=`00000000-0000-0000-0000-20a9e7000001`) 和 `Lex` (id=`00000000-0000-0000-0000-20a9e7000005`) 均 已绑 KB (新 deploy 后)
- KB · Mira 含 3 docs (灰度发布 / SLA / 隐私 review)
- KB · Lex 含 3 docs (物业管理条例 / 价格法 收费透明 / 民法典 业主权益)

## 2 · 预检 P-1 ~ P-2

### P-1 · 验证 Lex 已绑 KB (SSH 到 backend)

不可达 — Kimi sandbox 无 SSH 权. **改成 间接验证**: 跑 chat 看 kb_hits 是否 > 0.

### P-2 · 登录 leader

curl 走 cookie 流程:
```bash
curl -sc /tmp/cookies.txt $REPO_ROOT/scripts/login-helper.sh demo.lijg@futian.gov.cn demo123
```

**Pass**: cookie 含 session token, 后续 curl 走 `-b /tmp/cookies.txt`
**Fail**: 401 / 错密码

## 3 · 测试用例 T-01 ~ T-02

### T-01 · chat-A (Mira) KB hits > 0

**前置**: P-2 pass

**目标**: 重跑 `chat-A-mira.json` 3 turn, kb_hits_total 应 ≥ 1

**步骤**:
```bash
env -u HTTP_PROXY -u HTTPS_PROXY -u http_proxy -u https_proxy \
  python3 $REPO_ROOT/scripts/blind-test-chat-runner.py \
  --script $REPO_ROOT/docs/kimi-tests/blind-test/scripts/chat-A-mira.json \
  --email demo.lijg@futian.gov.cn \
  --password demo123 \
  --runner kimi \
  --out $REPO_ROOT/run-kimi-chat-A-round2.json
```

**注**: BASE_URL 在 runner 内 hardcode 为 `https://aimeeting.zhzjpt.cn` 不需 传.

**Pass 条件** (字面 metric):
- `chat_turns_count` = 3
- `chat_kb_hits_total` ≥ 1 (Round 1 是 0, 现在应 > 0)
- `chat_stance_violations` = 0
- 所有 turn 都拿到响应 (无 502 / timeout)

**Fail 条件**:
- `chat_kb_hits_total` = 0 (修复 没生效 — 可能 seed 漏跑 / 或 retrieve 阈值 0.55 太严)
- `chat_turns_count` < 3 (network 抖动)

**必须复述**: 输出 JSON 文件 完整 metric (用 jq 拉 5 个关键字段 + 完整 chat_kb_hits 字段)

---

### T-02 · chat-B (Lex 法务) KB hits ≥ 2

**前置**: P-2 pass, T-01 pass

**目标**: 重跑 `chat-B-lex.json` 3 turn 物业法规 场景, kb_hits_total 应 ≥ 2
(Lex 3 doc 都跟 物业 强相关, 命中率 应高于 Mira)

**步骤**:
```bash
env -u HTTP_PROXY -u HTTPS_PROXY -u http_proxy -u https_proxy \
  python3 $REPO_ROOT/scripts/blind-test-chat-runner.py \
  --script $REPO_ROOT/docs/kimi-tests/blind-test/scripts/chat-B-lex.json \
  --base-url https://aimeeting.zhzjpt.cn \
  --email demo.lijg@futian.gov.cn \
  --password demo123 \
  --output $REPO_ROOT/run-kimi-chat-B-round2.json
```

**Pass 条件**:
- `chat_turns_count` = 3
- `chat_kb_hits_total` ≥ 2 (3 turn 法规 场景 应 多 命中)
- `chat_stance_violations` = 0

**Fail 条件**:
- `chat_kb_hits_total` < 2 (Lex 法规 文档 retrieve 不命中, 可能 embedding 与 query 距离 > 0.55)

**必须复述**: 完整 metric JSON

---

## 4 · 报告模板

```
| 用例 | 状态 | Round 1 kb_hits | Round 2 kb_hits |
|------|------|-----------------|-----------------|
| T-01 (Mira) | PASS/FAIL | 0 | ?? |
| T-02 (Lex)  | PASS/FAIL | 0 | ?? |

结论: GREEN / YELLOW / RED
```

**GREEN**: 两个 都 ≥ 1 (Mira) / ≥ 2 (Lex)
**YELLOW**: Mira ≥ 1, Lex < 2 (KB 有 docs 但 retrieve 阈值 偏严)
**RED**: 仍 全 0 (KB seed 没生效 / 修复 失败)

## 5 · 反幻觉自检 (Kimi 提交前)

- [ ] kb_hits_total 用 字面 数字 复述, 不省
- [ ] 比较 Round 1 vs Round 2 用 真实 jq 数字, 不估算
- [ ] turn 失败 不允许 写 "应该 成功"

## 6 · 已知不在本次范围

- TTFC 优化 (中优, 留作 单独 saga, Round 1 已 GREEN 确认 backend 真问题, 但 不影响功能)
- 其他 8 个 agent (Sage / Stratos / 等) 也已 灌 KB, 但 Round 2 只跑 Mira + Lex (覆盖 Round 1 同剧本)
