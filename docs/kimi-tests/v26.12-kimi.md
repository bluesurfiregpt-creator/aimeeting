# v26.12 Kimi 用例 · 首页 重做(AI-first 卡片浏览)

> 版本: v26.12-Home (含 v26.12-fix1 nickname 衍生改动)
> 范围:
>   - **首页 主流程 重做** — 从 "新建会议长表单" 改成 "AI 卡片浏览 → 召唤"
>   - 加 `Agent.invoke_count` 字段, agent_router invoke 时 +1 (atomic UPDATE)
>   - 加 `Agent.nickname` 字段 (可选 拟人外号 — name=职务, nickname=拟人)
>   - `/api/agents` list 加 `q` / `sort` / `domain` / `active_only` query params
>   - 首页 搜索 + 最热/最新 toggle + domain chip 横滚 + **4-up grid 卡片** (lg+ 即 4 列, md 3 列, sm 2 列)
>   - 召唤 modal 二选一: 💬 私聊 (mock, 跳 /chat/<id>) / 🎤 邀请到会议 (创建 hybrid 跳 /meeting/<id>)
>   - 老 "新建会议 长表单" 抽 到 **`/meetings/new` 独立路由** (首页 留 一个 link card 跳过去)
>   - /me/profile/agents 编辑表单 加 `nickname` 输入框
>   - **v26.12-fix1 (nickname 衍生 改动)**:
>     - 后端 WS event (agent_message_start / agent_recommendation / dissent_detected / agenda_*) 全部 推 `agent_nickname`
>     - 后端 agent_router `_detect_at_mention` 同时 match name + nickname (@外号 也触发)
>     - 后端 LLM system prompt 加 一句 "你的 拟人外号 是 {nickname}" 让 LLM 自我认知
>     - 前端 AI 发言 bubble 显示 nickname 主 + name 副 (有 nickname 时)
>     - 前端 AI 画廊 / banner / 推荐卡 / dissent / moderator 全部 nickname 优先
> 测试者: Kimi
> 预计时长: 30 分钟

---

## 顶部 6 条死规矩 (反幻觉)

1. **只看真实页面 / 真实 HTTP 响应** — 每步 Pass/Fail 基于 浏览器 DOM 或 curl 输出
2. **每步独立判定** — 不允许 "按理说应该..." / "上一步通过这一步也应该过"
3. **复述要原文** — 引用 必须 带引号 + 实际渲染出来 的 字面字符
4. **不许跳步** — 前一步 fail 则 后续 BLOCKED
5. **不许编造账号** — 仅 §账号 表 5 个账号
6. **每用例 4 段** — (1) 实际看到 (2) 判定 pass/fail/blocked (3) 失败 理由 (4) 证据(截图 / JSON 原文)

---

## 环境 与 账号

**入口**:
- 根 URL: `https://aimeeting.zhzjpt.cn`
- 后端: `/api/agents` (含 query params)

**账号**:

| 角色 | 邮箱 | 密码 |
|------|------|------|
| owner | `bluesurfiregpt@gmail.com` | `<SYSTEM_OWNER_PWD>` |
| leader | `demo.lijg@futian.gov.cn` | `demo123` |
| admin | `demo.chensy@futian.gov.cn` | `demo123` |
| expert | `demo.fengl@futian.gov.cn` | `demo123` |
| member | `demo.hanx@futian.gov.cn` | `demo123` |

**前置假设**:
- workspace 有 ≥ 3 个 active expert agent (用于 卡片浏览测试)
- 至少 一个 agent 有 `domain` 字段非空 (用于 chip 筛选)

---

## 预检 P-1 ~ P-3

### P-1 · 生产健康
```bash
curl -sS -o /dev/null -w "%{http_code}\n" https://aimeeting.zhzjpt.cn
```
**Pass**: 200. **Fail**: 后续 BLOCKED.

### P-2 · 后端 API 活
```bash
curl -sS -o /dev/null -w "%{http_code}\n" https://aimeeting.zhzjpt.cn/api/auth/me
```
**Pass**: 401. **Fail**: BLOCKED.

### P-3 · owner 登录
**步骤**: 浏览器 `/login` → owner 账号 → 提交.
**Pass**: 跳到 `/`, 看到 大标题 "让会议拥有记忆与专家".
**Fail**: BLOCKED.

---

## T-01 · 首页 不再 是 "新建会议 长表单" ★ (架构反转 核心)

**前置**: P-3 pass.

**步骤**:
1. owner 登录后, 浏览器 在 `/` 根路径
2. 看 视口 第一屏 内容

**Pass** (必须 全部 满足):
- (a) 标题 "让会议拥有记忆与专家" 还在(没动)
- (b) 标题下面 **没有 "新建会议" 大表单** (议程行 / 真人 attendees 列表 / 模式 toggle 都 不在 视口)
- (c) 标题下方 有 一个 大的 搜索框 (placeholder 包含 "搜索 AI 专家")
- (d) 搜索框 下面 有 "🌟 专家" 二级标题 + 右侧 "最热 | 最新" 一组 toggle 按钮
- (e) 再 下面 有 domain chip 横排 (至少 看到 "全部" + 1 个 真实 domain 名)
- (f) 再 下面 有 一个 卡片 grid (≥ 1 张 卡片), 不是 checkbox 列表
- (g) **页面底部 有 一个 `<a>` link card** (不是 `<details>`), 包含 文字 "📦 新建 完整会议", `href` 指向 `/meetings/new`
- (h) 不存在 任何 折叠区 `<details>` — 老 form 完全 不 在 首页 渲染

**Fail 情景**:
- 视口 第一屏 看到 "议程项" / "勾选参会人" 等 老 form 字段 → 没拎到 /meetings/new
- 看不到 卡片 grid → 新主流程 没生效
- 底部 是 `<details>` 而 不是 link → 还停留 v26.12 初版 折叠区 设计

**复述**: 截 首屏 截图 ; 列 视口 内 看到 的 二级标题 + 折叠区 summary 文字 字面.

---

## T-02 · 卡片 7 件 信息 都 渲染 ★

**前置**: T-01 pass.

**步骤**:
1. 在 `/`, 看 卡片 grid 第一张 卡片
2. 把 鼠标 放 卡片上 (desktop) 触发 hover

**Pass** (单张卡 必须 含 全部):
- (a) 左上 头像 圆形 (48x48 左右, 有 头像 url 显图, 没 fallback 显 首字母)
- (b) 头像 右侧 第一行 显 全名 (文字 截断)
- (c) 头像 右侧 第二行 显 nickname (如 〈外号〉) 或 fallback domain
- (d) 头部下面 至少 1 个 domain chip (圆角 + 带颜色) — 如果 agent 有 domain
- (e) persona 截 3 行 描述 (line-clamp-3, 没 persona 显 "暂无介绍 — 由 manager 在 AI 配置页 补完.")
- (f) 卡片底部 左侧 显 "💬 N 次使用" (N >= 0 整数)
- (g) hover 卡片 → 右下角 "📣 召唤" 按钮 出现 (sm:opacity-0 → group-hover:opacity-100)

**Fail**:
- 任何 (a)~(g) 缺一 → fail, 列出 缺了哪个
- 召唤 按钮 一直 显示 / 一直 不显示 → group-hover 没生效 (移动端 应该 一直显, desktop 应该 hover 才显)

**复述**: 截 hover 状态 截图; 列 显 的 invoke_count 数字.

---

## T-03 · 后端 list_agents 新 query params 工作 ★

**前置**: T-01 pass, owner cookie 已拿到.

**步骤** (4 个 curl):

```bash
# 3a · 默认 (无参数)
curl -sS -b "<cookie>" https://aimeeting.zhzjpt.cn/api/agents | head -1
# 3b · 搜索
curl -sS -b "<cookie>" "https://aimeeting.zhzjpt.cn/api/agents?q=房屋"
# 3c · 排序 hot
curl -sS -b "<cookie>" "https://aimeeting.zhzjpt.cn/api/agents?sort=hot"
# 3d · domain 筛选
curl -sS -b "<cookie>" "https://aimeeting.zhzjpt.cn/api/agents?domain=楼宇安全&active_only=true"
```

**Pass**:
- (3a) 返回 JSON 数组, 每个 元素 含 `invoke_count` (number) + `nickname` (string|null) 两个 字段
- (3b) 仅 含 name/nickname/persona/domain 中 任一 包含 "房屋" 的 agent (其他 排除)
- (3c) 第一个 元素 的 invoke_count >= 第二个元素 的 invoke_count (降序)
- (3d) 仅 含 domain="楼宇安全" 且 is_active=true 的 agent

**Fail**:
- 字段 缺 `invoke_count` / `nickname` → schema 没 同步
- 搜索 返回 不相关 agent → ILIKE 没生效
- hot 排序 后 第一个 < 第二个 → ORDER BY 错

**复述**: 贴 (3a) 第一个 agent 的 完整 JSON 原文.

---

## T-04 · invoke_count 真的 +1 ★ (核心)

**前置**: T-03 (3a) pass — 拿 到 一个 agent 的 当前 `invoke_count` 值 (称作 N0).

**步骤**:
1. owner 新建 一场 hybrid 会议, 邀请 这个 agent (call 它 A)
2. 进 `/meeting/<id>`, 点 "▶ 开始会议", 等 ASR 连上
3. 在 会议室 AI 画廊 点 agent A 的 头像 (手动 invoke)
4. 等 5-10 秒, 看 agent A 的 流式回复 在 中栏 出现
5. 等回复 流式 结束 (流停 + 不再 加新 字)
6. curl GET `/api/agents/<A.id>` → 拿 当前 invoke_count (N1)

**Pass**:
- N1 = N0 + 1 (严格 +1, 不是 +2 不是 +0)

**Fail**:
- N1 = N0 → invoke_count 没 +1 (agent_router 改 没生效 / commit 失败)
- N1 = N0 + 2 → 重复 计数
- N1 > N0 + 1 → 多次 invoke 错累加

**复述**: N0 字面值 = ? N1 字面值 = ? 差值 = ?

---

## T-05 · 召唤 modal 二选一 + 私聊 跳 mock 页

**前置**: T-02 pass.

**步骤**:
1. owner 在 `/`, 鼠标 移 到 任意 卡片上, 点 "📣 召唤" 按钮
2. 看 modal 是否 弹出
3. modal 里 应该 有 2 个 大 选项 + 1 个 取消
4. 点 "💬 一对一 私聊" 选项

**Pass**:
- (a) modal 弹出, 标题 = "召唤 ${nickname 或 name}" (字面)
- (b) 第一个 选项 文字 = "一对一 私聊" + 带 "即将上线" amber chip
- (c) 第二个 选项 文字 = "邀请到 会议"
- (d) 点 私聊 → 跳 `/chat/<agent_id>` URL
- (e) /chat/<agent_id> 页面 显示:
  - 顶部 "← 返回 首页" 链接
  - AI 头像 + 名字 + 外号 + domain
  - 大 "🚧" + 标题 = "一对一 私聊 · 敬请期待"
  - 兜底 CTA "🎤 邀请到 新会议" 按钮

**Fail**:
- modal 弹不出来 → 召唤 onClick 没接
- 跳 /chat/<id> 后 404 → 文件路径 没建
- mock 页 不显示 "敬请期待" → mock 内容 没写

---

## T-06 · 召唤 → 邀请到会议 = 自动 创建 hybrid + prefill AI

**前置**: T-05 pass.

**步骤**:
1. 在 `/`, 召唤 任意 agent (称 它 B)
2. 点 modal 第二个选项 "🎤 邀请到 会议"
3. 等 modal 关掉 + 浏览器 跳到 `/meeting/<某uuid>`

**Pass**:
- (a) 跳到 `/meeting/<uuid>` 路径
- (b) 顶部 会议标题 = "与 ${B.nickname 或 B.name} 的对话" (字面)
- (c) AI 画廊 区 已 含 agent B 的 头像 (= attendee_agent_ids 已 prefill)
- (d) 后端 GET `/api/meetings/<uuid>` → JSON `attendee_agent_ids` 数组 含 B.id
- (e) JSON `mode` = "hybrid"

**Fail**:
- 没 跳到 `/meeting/<id>` → createMeeting 没成功
- attendee_agent_ids 不含 B.id → 前端 prefill 没传
- mode 不是 "hybrid" → 默认 mode 错

---

## T-07 · 搜索 + 防抖 + 排序

**前置**: T-01 pass.

**步骤**:
1. 在 `/`, 顶部 搜索框 输 "房" (一个字)
2. 等 1 秒 (防抖 300ms + 网络)
3. 看 卡片 grid 内容 变化

**Pass**:
- 卡片 只剩 名字 / 外号 / persona / domain 含 "房" 的 agent
- 浏览器 DevTools → Network → 看到 一个 GET `/api/agents?q=房&sort=hot&active_only=true` 请求

**步骤 (排序)**:
4. 清空 搜索, 点 "最新" toggle
5. 看 顺序 变化 + Network 是否 发 新请求

**Pass**:
- 卡片 顺序 重排
- Network 看到 GET `/api/agents?sort=new&active_only=true`

**Fail**:
- 输字符 没 debounce, 每个字符 都 发 请求 → 没接 防抖
- 切排序 没发 请求 → useEffect 依赖 漏了 sort

---

## T-08 · domain chip 横滚 筛选

**前置**: T-01 pass, 至少 一个 agent 有 domain (例 "楼宇安全").

**步骤**:
1. 在 `/`, 看 domain chip 行
2. 点 一个 具体 domain chip (不是 "全部")

**Pass**:
- 该 chip 视觉 选中 (accent 紫边 + 紫底)
- 卡片 grid 只 剩 该 domain 的 agent
- 点 "全部" → 恢复

**Fail**: 点 chip 卡片 不变 → 客户端 过滤 没生效.

---

## T-09 · /meetings/new 独立页 完整 form 能用 (回归)

**前置**: T-01 pass.

**步骤**:
1. 在 `/` 最下面, 点 "📦 新建 完整会议" link card
2. 浏览器 跳到 `/meetings/new`
3. 看 form 内容

**Pass**:
- (a) URL 是 `/meetings/new`
- (b) 顶部 有 "← 返回 首页" 链接, 点击 回 `/`
- (c) 页面 标题 = `"新建 完整会议"` (字面)
- (d) Form 含 全部 字段: 会议标题 input / 会议模式 toggle / 议程项 / 真人 attendees / 邀请 AI 多选 / "开始会议" 按钮
- (e) 字段功能 跟 v26.11 之前 一样 (不破回归)
- (f) 填表 创建 一个 hybrid 会议, 能 成功 跳 /meeting/<id>

**Fail**:
- 链接 404 → /meetings/new 路由 没建
- 字段 缺失 → form 没整体抽过来
- 创建 失败 → AdvancedMeetingForm 组件 行为 破回归

---

## T-10 · /me/profile/agents 编辑表单 含 nickname 输入

**前置**: P-3 pass.

**步骤**:
1. owner 进 `/me/profile/agents`
2. 点 任意 agent 进编辑
3. 找 "拟人外号" 字段

**Pass**:
- 编辑表单 含 一个 label = "拟人外号（可选，例 数妙妙 / 危叔，可空）" 的 input
- 改 nickname 为 "测试外号", 点 保存
- refresh, 该 agent 的 编辑表单 nickname 字段 = "测试外号"
- GET `/api/agents/<id>` JSON 中 nickname = "测试外号"
- 回 `/`, 该 agent 卡片 第二行 显示 〈测试外号〉

**Fail**:
- 没看到 nickname 输入框 → form 没加
- 保存后 GET 仍 nickname=null → PATCH 没传 / 后端 没接

---

## T-11 · 4-up grid 在 桌面 真的 4 列 ★

**前置**: T-01 pass. workspace 有 ≥ 4 个 active agent.

**步骤**:
1. 浏览器 窗口 拉宽 (≥ 1024px 桌面 宽度)
2. 看 卡片 grid 一行 几个 卡片

**Pass**:
- 一行 显示 4 个 卡片 (lg:grid-cols-4)
- 第 5 张 自动 换行 第 2 行 第 1 列

**Fail**:
- 一行 只 3 张 → grid 配置 错 (xl 起 4 列 而 不是 lg 起)

---

## T-12 · @nickname 触发 AI 发言 ★ (#4 衍生 核心)

**前置**: 找 一个 agent 有 nickname (e.g. owner 给 一个 agent 设 nickname = "测试外号").
该 agent 邀请 到 一场 进行中 hybrid 会议.

**步骤**:
1. 进 会议室, 启动 录音 + WS
2. 在 文字输入框 输入 `@测试外号 你好` (用 nickname, 不是 name)
3. 提交

**Pass**:
- AI bubble 出现, 流式 回复 (= @nickname 真的 触发 了 调用)
- 后端 log (可选 SSH 看): 显示 "agent invoked by at_mention"
- bubble 头部 标签 显 nickname 主 + name 副 (例 `"测试外号 ｜ <真实name>"`)

**Fail**:
- 输入 @nickname 后 AI 不回 → _detect_at_mention 没 接 nickname
- bubble 仍 只显 name → WS event push 没 含 agent_nickname / 前端 没渲

---

## T-13 · AI 发言 bubble nickname 主 + name 副 ★ (#3)

**前置**: T-12 pass (AI 已 发言 至少 一次).

**步骤**: 看 中栏 AI 发言 卡片 头部.

**Pass**:
- 头部 显示 拟人外号 (e.g. "测试外号") 作为 大字 主标
- 副标 显示 完整 职务 name (e.g. "测试外号 ｜ <真实name>") 或 副位 一行
- 如果 该 agent **没有 nickname**, 仅 显示 name (回归 — 不破老行为)

**Fail**:
- 仅 显示 name 即使 nickname 存在 → bubble render 没改

---

## T-14 · AI 画廊 卡片 显示 nickname 优先 (#5)

**前置**: agent A 已邀请 + 有 nickname; agent B 已邀请 + 无 nickname.

**步骤**:
1. 在 会议室, 看 顶部 AI 画廊
2. 看 agent A / B 头像 下面 文字

**Pass**:
- agent A: 第一行 显 nickname, 第二行 灰小字 显 name
- agent B: 第一行 显 name, 第二行 灰小字 显 domain (跟 v26.10 一样)

**Fail**:
- agent A 显 name → 画廊 没接 nickname 优先

---

## T-15 · LLM 系统 prompt 含 nickname (#6 — 行为 验证)

**前置**: agent C 有 nickname = "测试外号" + persona (含 自我介绍).
拉一场 hybrid 会议邀请 C.

**步骤**:
1. 启会议, 在 文字框 输入 `你的拟人外号 是 什么?`
2. 等 关键词 / @-mention 不一定 触发 — 改 用 手动 invoke (点 画廊 头像)
3. 看 AI 回复 内容

**Pass**:
- AI 回复 中 提及 "测试外号" 或者 它 知道 自己 有 这个 别名 (LLM 自我认知)
- 至少 不 矛盾说 "我只 叫 <name>, 没 别名"

**Fail (注: LLM 行为 ≠ 严格 字面, 容忍 弱判定)**:
- AI 完全 不认 nickname / 说 "我不叫测试外号" → system prompt 没拼 nickname

**注意**: 此用例 弱判定, LLM 输出 难严格. 写明 "LLM 没认 → 软 fail, 报警 但 不 RED".

---

## 报告模板

```markdown
# v26.12 Kimi 报告

## 总结
- T-01 ~ T-15: x/15 PASS / y FAIL / z BLOCKED
- 结论: GREEN (全 pass) / YELLOW (1-2 fail 非核心) / RED (T-01 / T-04 / T-05 / T-06 / T-12 / T-13 任一 fail)

## 用例明细
### T-01 · 首页 架构反转
- 实际看到: ...
- 判定: ...
- 失败理由: ...
- 证据: ...

(... T-02 ~ T-10 同样格式)

## 关键证据 复述
| 测试 | 实际 字面值 | 字面常量 一致 |
|------|------------|---------------|
| T-01-d | 二级标题 实际 = "..." | ✅ / ❌ |
| T-03-a | agent JSON 含 invoke_count + nickname | ✅ / ❌ |
| T-04 | N0 = ?, N1 = ?, 差 = ? | +1 ✅ / 其他 ❌ |
| T-05-a | modal 标题 实际 = "..." | ✅ / ❌ |
| T-06-b | 会议标题 实际 = "..." | ✅ / ❌ |
```

---

## 反幻觉 自检

- [ ] 没 写 "应该 / 估计 / 看起来"
- [ ] 字面值 完整, 没 翻译 (不 把 "邀请到 会议" 写成 "Invite to meeting")
- [ ] T-04 N0 / N1 都 是 curl 实跑 拿 的, 不是 推算
- [ ] T-02 hover 截图 真的 显出 召唤按钮 (不是 没 hover 截 然后 说 "应该 hover 会出现")

---

## 已知 不在 v26.12 范围

- 私聊 真实现 (现在 是 mock 页) — v26.13 单独 做
- 专家团 (多 AI 套餐) 板块 — v26.13 再做
- 邀请到 已有 进行中会议 (现在 召唤 = 创建 新会议) — 后期 加 fallback
- 卡片 上 直接 拖拽 多选 → 组队 — 未规划

**END.**
