# Kimi 测试用例 · v1.2.0 Saga · meeting-room-v2

> **变更内容**: 移动端会议室 (`/m/meetings/[id]/`) 整页浅色化 + iOS 风重写
> **Scope**: 仅这一页 + 它专用的 mobile 组件; 其他页 / globals / backend / 小程序 一律 不动
> **commit**: (待 主 agent 提交)
> **部署地址**: `https://aimeeting.zhzjpt.cn`
> **测试对象**: Kimi (AI 自动化测试, 非人工)
> **本次目标**: 验证浅色 iOS 风会议室能正确加载, 顶栏 + 议程 strip + 参会人 strip + 双行 dock + 5 sheet + AI 圆桌 mock + WS 不挂

---

## 0. 给 Kimi 的 6 条死规矩 (必读, 违反任何一条整份报告作废)

1. **只看真实页面 / 真实 HTTP 响应**. 任何 "按经验, 应该", "通常这种页面会有 X" 都是幻觉, 禁用.
2. **每一步的判定必须基于本步骤实际看到的截图 / DOM 文本 / HTTP 状态码 / JSON 字段**; 不允许 推测, 不允许 复用上一步结论.
3. **复述要原文**. 若用例要求复述按钮上的字, 必须 复制粘贴 `@ AI 专家` 这种带引号的精确字符串, 不能改写成 "召唤 AI" 或 "AI 专家按钮".
4. **不允许跳步**. 每个用例的步骤是顺序依赖的; 前一步 fail 则后续步骤标记 `BLOCKED`, 不允许 "我猜应该是这样" 继续测.
5. **不允许 编造账号**. 本份用例里出现的所有账号、密码、URL、按钮文字 都是字面常量, 任何 `demo@example.com` / `password123` / 类似的占位都是错的.
6. **每个用例必须给完整 4 段**: `实际看到 / 判定 (pass|fail|blocked) / 失败理由 (若 fail) / 证据 (截图路径或 curl 原文)`, 缺一段 = fail.

---

## 1. 环境 与 账号

### 1.1 入口
- 网站根: `https://aimeeting.zhzjpt.cn`
- 移动端工作台: `https://aimeeting.zhzjpt.cn/m`
- 会议列表: `https://aimeeting.zhzjpt.cn/m/meetings`
- 会议室: `https://aimeeting.zhzjpt.cn/m/meetings/<meeting_id>` (实际 meeting_id 从列表点进去)

### 1.2 测试账号 (唯一, 不要乱编)

| 角色 | 邮箱 | 密码 | 备注 |
|------|------|------|------|
| owner / 召集人 | `bluesurfiregpt@gmail.com` | `<SYSTEM_OWNER_PWD>` | workspace 拥有者 |

**说明**: 本次用例只需要这一个账号 — 进自己 workspace 的任意一场 ongoing/finished 会议即可. 如果你看到要求登录别的账号, 本份用例 STOP.

### 1.3 数据库前置假设 (本份用例依赖这些事实, 如不成立则整份 BLOCKED)
- workspace 内 **至少有 1 场 `status='ongoing'`** 的会议 (从 `/m/meetings` 列表点进去能直接看到会议室)
- 该会议 **至少邀请了 1 个 AI 专家** (用于 `attending_agents` 非空, "@ AI 专家" sheet 内有内容)
- owner 账号能登录, 移动端能进 `/m`

> 若不满足: 用任意手段先在 PC 端创建一场 ongoing 会议 + 邀请 ≥1 AI 专家, 再来跑用例.

### 1.4 怎么进会议室页 (Kimi 必看, 不要乱猜路径)

1. 在浏览器 把 user-agent 切到移动端 (iPhone Safari) 或 DevTools toggle device toolbar
2. 打开 `https://aimeeting.zhzjpt.cn/m` 登录 owner
3. 点底部导航的 "会议" tab → 打开 `/m/meetings`
4. 点列表中任意一场 `status=ongoing` 的会议卡片 → 跳转 `/m/meetings/<id>`
5. 看到的就是本 Saga 重写的页面

---

## 2. 预检 P-1 ~ P-4 (必须全过, 任何 fail 则后续 T-* 全 BLOCKED)

### P-1 服务器健康
**HTTP 请求**: `curl -sS -o /dev/null -w "%{http_code}\n" https://aimeeting.zhzjpt.cn/`
**Pass 条件**: 返回值 字面等于 `200`
**复述**: 粘贴 curl 实际输出的那一行
**Fail 处理**: 整份 BLOCKED, 报 "服务器不可达"

### P-2 移动端入口活
**HTTP 请求**: `curl -sS -o /dev/null -w "%{http_code}\n" https://aimeeting.zhzjpt.cn/m`
**Pass 条件**: 返回 字面等于 `200` 或 `307` (前者 = 已登录页面, 后者 = 重定向到 login)
**Fail 处理**: 若 5xx → BLOCKED

### P-3 后端 API 活
**HTTP 请求**: `curl -sS -o /dev/null -w "%{http_code}\n" https://aimeeting.zhzjpt.cn/api/agents`
**Pass 条件**: 返回 字面等于 `401` (未登录情况下 backend 返回 401 是正常)
**Fail 处理**: 5xx → BLOCKED; 200 → workspace 路由配置异常, STOP

### P-4 登录到会议室
**步骤**:
1. 浏览器 移动端模式打开 `https://aimeeting.zhzjpt.cn/m`
2. 若看到 login 表单, 用 1.2 表里的 owner 凭据登录
3. 点导航 "会议" → 列表 → 任意 ongoing 会议 → 进入会议室
4. **不要急着进入用例 T-01, 先看页面到底渲染出来了没**

**Pass 条件**:
- 页面顶部出现 ← `历史` 按钮 (蓝色文字 `历史`)
- 页面顶部中央有 字面文字 `实时` (红色) + 一个 `mm:ss` 格式的 timer
- 主区域 **背景色是浅色** (不是深色) — 用 DevTools 查 `body` 的 `bgcolor` 或 截图判断接近 `#F2F2F7`

**复述**: 整页截图 + 顶栏的 timer 数字 + 背景色十六进制 (从 DevTools)

**Fail 处理**:
- 看不到 ← 历史 按钮 / `实时` 文字 → fail "页面没渲染或老版"
- 背景接近 `#0b0d12` / `#0c0e14` 等深色 → fail "浅色化未生效"

---

## 3. 测试用例 T-01 ~ T-10

### T-01 整页浅色 + iOS 字体

**前置**: P-1~P-4 全 pass
**目标**: 验证整页是浅色 iOS 风格, 不是旧 dark

**步骤**:
1. 进入会议室页 `/m/meetings/<id>`
2. 在 DevTools 中读取主容器 (`div[style*='fixed']`) 的 `background` 或 `body` 的 `bgcolor`
3. 在 DevTools 中读取主容器的 `font-family` 计算值
4. 截图整页

**Pass 条件**:
- 主容器背景 字面为 `rgb(242, 242, 247)` 或 `#F2F2F7` (允许 `(242,242,247)`)
- 字体栈 字面包含 `-apple-system` 或 `SF Pro Text` 关键字
- 截图: 顶栏白底 + 议程 strip 白底 + transcript 区灰底, 不是深色

**Fail 条件**:
- 主容器背景 = `rgb(11,13,18)` (旧 dark `ink-950`) → fail "浅色化未上线"
- 字体计算值不含 `apple-system` → fail "iOS 字体未注入"

**必须复述**:
- DevTools 读到的 background-color 完整字符串
- font-family 完整字符串
- 截图路径

---

### T-02 顶栏 4 槽 ID + 实时红点

**前置**: T-01 pass
**目标**: 验证 MRHeader 4 槽 (← 历史 / 标题+实时+timer / 章节 / 筛选) 都在

**步骤**:
1. 在会议室页面上, 找页面顶部最上方的横条 (有边界 hairline 分隔下面的议程 strip)
2. 数横条上的可点击元素
3. 截图横条

**Pass 条件**:
- 横条最左: 1 个按钮, 内容 字面是 `← 历史` (一个 svg 箭头 + 文字 `历史`)
- 横条中央: 会议标题 (1 行) + 下方一行小字 字面包含 `实时` 红色 + 一个 mm:ss timer 灰色
- 横条最右: 2 个 svg 图标按钮 (章节 + 筛选)
- 实时红点应在闪烁 (有 livePulse 动画)

**Fail 条件**:
- 没有 `历史` 文字 → fail "← 按钮文字漂移"
- 中央没看到 字面 `实时` → fail "live 状态指示缺失"
- 中央 timer 不是 `mm:ss` 格式 (e.g. 是 `12 分钟` 或 `12m`) → fail "timer 显示格式错误"
- 只有 1 个右侧 icon (没章节或没筛选) → fail "header 槽位不全"

**必须复述**:
- 顶栏所有可见文字的精确拷贝 (含 `历史`, `实时`, timer 数字)
- 截图

---

### T-03 议程 strip + segmented progress bar

**前置**: T-02 pass
**目标**: 验证议程 strip 不再是横滑 chip, 是 "议程 X/N · title · 剩余 N 分钟" + 下方分段 progress

**步骤**:
1. 在顶栏正下方找议程 strip 横条
2. 找文字: `议程 X/N` 风格 (e.g. `议程 2/4`) + 议程标题 + `剩 N 分钟`
3. 找下方一组细横条 (segmented bar)
4. 截图

**Pass 条件**:
- 出现 字面 `议程 ` + 数字 + `/` + 数字 (regex `议程\s*\d+/\d+`)
- 出现 议程标题文字
- (议程 1 已结束时) 出现 字面 `剩 N 分钟` 或议程已完成时 `议程已全完成`
- 下方分段 bar 至少 2 段 (议程项数 ≥ 2)
- 已完成段 应为绿色 (rgb 接近 `#34C759`), 当前段应为蓝色 (rgb 接近 `#007AFF`)

**Fail 条件**:
- 看到旧版横滑 chip (圆角 ring + ✓ / ● / ○ 图标) → fail "AgendaStrip 没换新版"
- 没看到 `议程 X/N` 字串 → fail "议程编号丢失"
- 分段 bar 缺失 → fail "progress bar 没渲"

**必须复述**: 完整的 "议程 X/N · title · 剩 N 分钟" 字符串

---

### T-04 参会人 strip — 3 型头像区分

**前置**: T-03 pass
**目标**: 验证 ParticipantsStrip 横滑头像列, 含 host (同心圆) + humans (圆形) + ais (渐变方形)

**步骤**:
1. 在议程 strip 正下方找一行 横滑头像
2. 找头像列上方的 caption 字面: `参会 · N 人 + M AI 专家`
3. 数头像 — 应至少有 1 个 host (Mira 同心圆) + 5 个 mock humans (圆形彩色) + ≥ 1 个真实 AI 专家 (渐变方形)
4. 截图

**Pass 条件**:
- 上方 caption 包含 `参会` + `人` + `AI 专家`
- 第一个头像 是同心圆 (圆形, 中间是橙色, 外圈白圈套橙环) — Mira 主持人专属
- 第一个头像下方文字 字面 = `Mira`, 第二行小字 `主持人`
- 后续有圆形彩色头像 (字母首字) — humans
- 至少一个渐变方形圆角头像 (含白色 sparkle SVG) — ai
- 头像区可横向滑动

**Fail 条件**:
- 三型头像样式不区分 (例如全是圆形, 或全是方形) → fail "avatars 没换新版"
- 看不到 Mira 字面 → fail "host 没渲"

**必须复述**: 头像列上方 caption 完整字符串 + 第一个头像下方的 `Mira` 文字 + 1 个 ai 头像下方的名字 (e.g. `Aria`)

---

### T-05 Dock 双行 — Row1 大紫 + 大琥珀, Row2 6 控制按钮

**前置**: T-01 pass
**目标**: 验证底部 dock 双行结构, 不是旧版 sticky bar

**步骤**:
1. 滚动到页面底部, 找浮在底部的 dock (白底圆角)
2. Row 1 — 应有 2 个大按钮:
   - 左: 紫色渐变, 内含 sparkle 图标 + 字面 `@ AI 专家`, 小字 `唤醒领域专家答复`
   - 右: 琥珀色渐变, 内含 Mira 同心圆头像 + 字面 `问主持人`, 小字 `拆解 · 路由 · 议程`
3. Row 2 — 应有 6 个小方块按钮, 从左到右:
   - `麦克风` / `已静音` (mic icon)
   - `摄像头` / `已关闭` (video icon)
   - `举手` / `举手中` (hand icon)
   - `字幕` (cc icon)
   - `更多` (more icon)
   - `结束` (end icon, 红色背景)
4. 截图整个 dock

**Pass 条件**:
- 上面所有按钮字面与位置全对齐
- 左大按钮背景是紫色渐变 (含 `#5E5CE6` 紫)
- 右大按钮背景是琥珀色渐变 (含 `#FF9F0A` 橙)
- 右大按钮内嵌的是 Mira 同心圆头像 (不是 sparkle 图标)
- 结束按钮背景红色 (`#FF3B30`)

**Fail 条件**:
- 看到 1 个按钮 `推进议程` 或 `召唤专家加视角` 字面 → fail "旧 StickyActionBar 还在跑"
- Row1 缺失大按钮 → fail "双行 dock 没生效"

**必须复述**:
- Row 1 两个大按钮的文字 (含小字描述)
- Row 2 六个按钮的文字 (从左到右)

---

### T-06 点 `@ AI 专家` 弹浅色 sheet

**前置**: T-05 pass
**目标**: 验证 SummonAgentSheet 浅色化 + 渐变方形头像

**步骤**:
1. 点击 dock 上的 `@ AI 专家` 大紫色按钮
2. 看是否从底部弹出一个 sheet
3. 看 sheet 顶部的把手 (拖动条)
4. 看 sheet 标题
5. 看 sheet 内部的 AI 列表行

**Pass 条件**:
- sheet 从底部滑出 (有 slideUp 动画)
- sheet 顶部有一个 36×5 像素的灰色把手 (圆角胶囊)
- 标题 字面 = `唤醒 AI 专家`
- agent 行的头像是 **渐变圆角方形** (含白色 sparkle SVG), 不是圆形色块
- sheet 背景接近 `#F2F2F7` 浅色, 不是 dark
- 顶部有 `完成` 按钮 (蓝色文字)
- 底部有 `取消` 按钮 + 一个 紫色渐变 `召唤 <name>` 大按钮 (或 `选一位专家` 灰色)

**Fail 条件**:
- 弹出的是 dark sheet → fail "SummonAgentSheet 没换新版"
- 头像是圆形 ◆ 字符 → fail "MRAIAvatar 未生效"

**必须复述**: sheet 标题原文 + 第一个 agent 行的 nickname/name 文字 + 底部 button 原文

---

### T-07 点 `问主持人` 弹 Mira sheet

**前置**: T-05 pass
**目标**: 验证 AskHostSheet 浅色化 + Mira 头像 + 6 个 quick chips + textarea

**步骤**:
1. 点击 dock 上的 `问主持人` 大琥珀色按钮
2. 看弹出的 sheet
3. 数 quick chips
4. 看 textarea

**Pass 条件**:
- sheet 标题 字面包含 `问主持人 Mira` (Mira 同心圆头像 + 文字 一同)
- sheet 顶部有琥珀渐变的 self-intro 卡, 字面包含 `管议程` 或 `路由` 关键字
- 中间区域字面 `常用请求` + 2 列 6 个 chip
- 6 个 chip 中至少出现 字面 `本议程还剩多久?` (这是写死的 mock)
- 底部 textarea 占位文字包含 `B 组延迟` (mock placeholder, regex `B\s*组.*延迟`)
- 底部 `按住说话` button + `发送` button

**Fail 条件**:
- 没弹任何 sheet → fail "onAskHost 没接好"
- 弹了 SummonAgentSheet → fail "两个 PrimaryBtn 串了"
- 字面 `Mira` 没出现 → fail "host 命名漂移"

**必须复述**: sheet 标题原文 + 6 个 chip 中的至少 3 个 chip 完整文字 + textarea placeholder 原文

---

### T-08 AI 圆桌 mock 在 transcript 末尾出现 + 紫色 "演示" 角标

**前置**: T-01 pass
**目标**: 验证 mock round 永久 1 张固定卡, 头部有紫色 "演示" 角标 (R3)

**步骤**:
1. 在 ongoing 会议室页, 把 transcript 滚到最底部 (主区域 scroll 到底)
2. 找一张 卡片, header 内含字面 `AI 圆桌` (e.g. `AI 圆桌 · 3/3 已答`)
3. 卡片 header 内应同时出现 字面 `演示` (紫色小角标)
4. 卡片 body 内含 quote 字面包含 `B 组灰度` (mock data 的 topic)
5. 内嵌 Mira 综合块 + 3 个手风琴

**Pass 条件**:
- 字面 `AI 圆桌` 出现
- 紫色 `演示` 角标可见 (背景紫色渐变 `#AF52DE → #5E5CE6`)
- topic 文字含 `B 组灰度`
- 手风琴 (默认全收起) 共 3 个, 分别是 `Aria` / `Lex` / `Sage` (mock 6 名中的 3 个)
- Mira 综合块的 verdict 字面 = `可推进 · 注意法务节奏`
- 至少 1 个手风琴行右侧有 `chev` 箭头 icon

**Fail 条件**:
- transcript 底部没看到圆桌 → fail "mock round 没插入"
- 没看到 `演示` 角标 → fail "R3 mitigation 漏做, 用户会误把 mock 当真功能"

**必须复述**:
- 圆桌 header 内的完整字符串 (`AI 圆桌 · 3/3 已答`)
- topic 完整字符串
- Mira verdict 完整字符串

---

### T-09 圆桌手风琴 — 一次只展开一位 + 详细数据/记入决策按钮

**前置**: T-08 pass
**目标**: 验证 RoundMessage 内的 ExpertAccordion 互斥 + 详细数据/记入决策 按钮点了出 toast

**步骤**:
1. 点击第一个手风琴 (e.g. Aria 行)
2. 看是否展开, 显示 summary + 3 个数据小块 + note 区
3. 点击第二个手风琴 (e.g. Lex 行)
4. 看 Aria 是否自动收起 + Lex 展开
5. 展开后 找最底部的 `详细数据` / `记入决策` 按钮
6. 点 `记入决策` 按钮
7. 看是否弹出 toast 包含 `后续上线` / `demo` / `暂未` 关键字 (按 page 的 `onDemoAction` toast 文案)

**Pass 条件**:
- 一次只 1 个 expert 展开 (互斥)
- 展开后 至少 3 个 数据小块 (label + 数字 v)
- 数据小块的数字 字面是 mock 数据 (e.g. Aria 的 `+11.4pp` / `1.18s` / `9pp` 等, copy from mock/roundtable.ts)
- `详细数据` / `记入决策` 按钮可见
- 点 `记入决策` 后 出 toast (文字含 "后续" 或 "暂未" 类 demo 关键字 — 父级 onAction 没接, 暂用 onMoreSheet 默认 toast 或 sheet 内 onDemoAction toast 实现, 任一即可)

**Fail 条件**:
- 两个手风琴同时展开 → fail "互斥失效"
- 点了 `记入决策` 没反应 (无 toast / 无错) → fail "demo action 没接"

**必须复述**: 第一个手风琴展开后的 summary 文字 + 数据小块 v 字段 (3 个) + toast 文字

---

### T-10 结束会议 iOS 居中弹窗

**前置**: T-05 pass, dock 上的 `结束` 按钮可见
**目标**: 验证 EndConfirm 是 iOS 居中 280px 弹窗, 不是旧 ConfirmDialog

**步骤**:
1. 点击 dock 上的 `结束` 红色按钮
2. 看是否弹出居中 modal (不是底部 sheet)
3. 测 modal 宽度 接近 280px
4. 看 modal 标题 + body 文字
5. 看底部按钮

**Pass 条件**:
- modal 弹在 屏幕居中 (transform translate(-50%, -50%))
- 宽度 接近 280px (允许 250 ~ 320)
- 背景接近 `#F5F5F7` 透明 (有 backdrop blur)
- 标题 字面 = `结束会议?`
- body 字面包含 `主持人 Mira` + `AI 摘要` + `决策项` + `行动项`
- 底部 hairline 分隔 双键: 左 `取消` (蓝色), 右 `结束` (红色 + bold)

**Fail 条件**:
- 弹出来的是底部 sheet (sheet 风格, 顶部把手) → fail "EndConfirm 没生效"
- 标题不是 `结束会议?` → fail "文案漂移"

**必须复述**: modal 完整宽度 + 标题原文 + body 原文 + 两个 button 原文

---

### T-11 (额外) 章节 sheet + 筛选 sheet 能弹

**前置**: T-02 pass
**目标**: 验证顶栏右侧 章节 + 筛选 两个 icon 按钮 onClick 弹对应 sheet

**步骤**:
1. 点 顶栏右侧 `章节` icon (倒数第二个)
2. 看是否弹出 `章节 · 重要时刻` sheet
3. 关掉 (点 `完成`)
4. 点 顶栏右侧 `筛选` icon (倒数第一个)
5. 看是否弹出 `筛选发言` sheet

**Pass 条件**:
- 章节 sheet 标题 字面 = `章节 · 重要时刻`
- 章节 sheet 内部有 highlights 列表 (至少 1 个 mock round 节点 — 因 page 把 mock round 加进了 highlights)
- 筛选 sheet 标题 字面 = `筛选发言`
- 筛选 sheet 顶部左侧有 `清空` 按钮 (灰色, disabled 状态因 selected 为空)
- 筛选 sheet 内部分 3 段: `主持人` / `团队成员` / `AI 专家`
- AI 专家段下 至少有 6 个 mock AI 行 (Aria / Stratos / Lex / Sage / Tally / Scout) 或更多 (含真实 attending_agents)

**Fail 条件**:
- 章节 / 筛选按钮没反应 → fail "onClick 没接"
- 章节 sheet 没看到 mock round 节点 → fail "highlights 没注入"

**必须复述**: 两个 sheet 标题原文 + 筛选 sheet 内 6 个 mock AI 名字 (复述 ≥ 4 个即可)

---

## 4. Kimi 报告模板 (按这个回, 不许偏)

```
# 测试报告 · v1.2.0 Saga · meeting-room-v2

## 总结
- pass / fail / blocked 计数: X / Y / Z
- 结论: GREEN / YELLOW / RED  (≥10 pass = GREEN, 1-2 fail = YELLOW, ≥3 fail 或 任何 P-* fail = RED)

## 预检明细
P-1 ... (实际看到 / 判定 / 失败理由 / 证据)
P-2 ...
P-3 ...
P-4 ...

## 用例明细
T-01 ... (4 段)
T-02 ... (4 段)
...
T-11 ... (4 段)

## 异常 / 观察 (可选)
(看到 changelist / TD 外的 UI 异常 时填这里; 没看到 写 "无")
```

---

## 5. 反幻觉自检清单 (Kimi 提交报告前自查)

- [ ] 没截图的步骤 **不允许** 声称 "看到"
- [ ] 不允许 把 `背景接近浅色` 当成 `背景 = #F2F2F7` (必须 DevTools 读字面 hex)
- [ ] 不允许 出现 `应该 / 通常 / 估计 / 似乎` 这类副词
- [ ] 不允许 把 `AI 圆桌 · 3/3 已答` 写成 `圆桌已完成`
- [ ] 复述的字符串 必须 用 反引号 包裹 (例: `@ AI 专家`), 不允许 改写成 `召唤AI`
- [ ] WS event-driven 卡 (T-01 ~ T-05 / T-08) 在 ongoing 会议中跑; finished 会议 默认不会弹 banner — 若用 finished 会议测 T-01 / T-08, 必须 在报告里写明 `测的是 finished 会议, banner 不出是预期`

---

## 6. 已知不在本 Saga scope (Kimi 别误报)

| 现象 | 说明 |
|---|---|
| `/m` 工作台仍是 dark | F1 视觉撕裂, 下个 Saga 处理 |
| `/m/meetings` 列表 dark | 同上 |
| AI 圆桌的 `详细数据 / 记入决策` 按钮点了仅 toast | 设计就是 mock — toast 文字含 "后续上线" 或类似 |
| 章节 / 筛选 sheet 的数据是 mock | TD8 — 后端无 chapter/highlight 提取 API, 本 Saga 仅 UI |
| Mira 角色头像在其他页面看不到 | F3 接受 — 仅会议室页内出现 |
| BottomNav (`/m/meetings`, `/m/tasks` 等) 仍 dark | TD10 接受 — 会议室页 `fixed inset-0` 覆盖了它 |
| 历史会议 (status=finished) 也走新浅色 UI 但没 host card / round | F4 — finished 不会触发新 WS event; mock round 仍显 |
| 真人筛选段为空 | 现 backend 无 attendee 真人列表 API; 仅显 AI 专家段 |
| 字幕 / 摄像头 / 举手 等 Row2 按钮无后端联动 | 仅 UI toggle (本 Saga 不接硬件) |
| 录音状态 / 麦克风权限 | TD5 — 录音状态合进 mic CtrlBtn, 但 实际硬件接入留下一 Saga |

---

## 7. 部署校验脚本 (Kimi 跑完用例后, 用这个 curl 复核)

```bash
# 1. 主 HTML 含本 Saga 关键字符串
curl -sS https://aimeeting.zhzjpt.cn/m/meetings/<any-id> | grep -oE 'meeting-room-v2|MR_FONT_FAMILY|MRHostAvatar' | head -3

# 2. 旧 dark sticky 关键字符串 应该 已不在 page bundle 中
# (Next.js client bundle 已 minify, 但 source map 里仍含; 不强查, 跳)
```

> 若 (1) curl 输出 = 空, 说明源码字符串被 tree-shaken 干净 — 也可能页面没部署, 用浏览器再确认一次再下结论.
