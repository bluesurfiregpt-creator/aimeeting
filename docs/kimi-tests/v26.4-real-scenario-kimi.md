# Kimi 测试用例 · v26.4 真实场景综合验证

> **场景**:乙方运营给新客户开 workspace → 客户激活账号 → 客户开会议 → 超管切进客户空间运维 → 错误提示分类验证
>
> 🚨 **环境前提**:
> - 服务器 `backend/.env` 已配 `PLATFORM_ADMIN_EMAILS=bluesurfiregpt@gmail.com`
> - DB 已清干净:**1 个 workspace(默认)+ 1 个 user(超管)+ 21 个 agent + 20 个 KB**
> - 任何 Kimi 注册 / 创建出的临时账号 / workspace 都是 本次测试新建,不污染已有数据
>
> **测试目标**(综合 4 项):
> 1. v26.4 平台超管 list / create / switch / audit 端到端
> 2. v26.4-fix1/fix2/fix3 跨 workspace 视角 ABAC 无误伤
> 3. v26.4-fix4 错误提示分类(4 种 toast 颜色 + 文案)
> 4. 一次性邀请链接 + 临时密码 真能用

---

## 0. 6 条死规矩(必读)

1. 只看真实页面 / 真实 HTTP 响应.推测一律 fail.
2. 每步判定基于本步实际数据,不复用上一步.
3. 复述要原文带引号(`role: "owner"` / `phase: "running"`).
4. 顺序依赖.前置 fail → 后续 BLOCKED.
5. 不允许编账号 / URL / 字段名 — 用 §1.2 的字面常量.
6. 每用例必须给 4 段:实际看到 / 判定 / 失败理由 / 证据.

**额外**:本次综合用例 你 会**自己创建客户账号**(不是事先 seed 的).这些账号你 创建时 自己 记下来,后续用例引用.

---

## 1. 环境 + 账号

### 1.1 入口
`https://aimeeting.zhzjpt.cn`

### 1.2 起步账号

| 角色 | 邮箱 | 密码 |
|---|---|---|
| 平台超管(=默认 ws owner) | `bluesurfiregpt@gmail.com` | `<SYSTEM_OWNER_PWD>` |

### 1.3 测试过程会创建的账号(由你决定具体值)

| 用途 | 邮箱模板 | 角色 |
|---|---|---|
| 客户单位 owner | `kimi-client-<UNIX 时间戳>@example.com` | owner of 新建 客户 ws |
| 客户单位 admin | `kimi-admin-<UNIX 时间戳>@example.com` | admin(通过 owner 邀请) |
| 客户单位 expert | `kimi-expert-<UNIX 时间戳>@example.com` | expert + 必填 bound_agent_id |
| 客户单位 member | `kimi-member-<UNIX 时间戳>@example.com` | member |

**起 4 个浏览器/隐私窗口**:超管 + 客户 owner + 客户 expert + 客户 member.记好哪个窗口对应哪个账号.

---

## 2. 预检 P-1 ~ P-4(任一 fail → 后续 BLOCKED)

### P-1 服务可达
```bash
curl -sS -o /dev/null -w "%{http_code}\n" https://aimeeting.zhzjpt.cn/
```
**Pass**:字面 `200`

### P-2 后端 401
```bash
curl -sS -o /dev/null -w "%{http_code}\n" https://aimeeting.zhzjpt.cn/api/agents
```
**Pass**:字面 `401`

### P-3 超管登录 + 顶栏 ⚡ 入口
1. 浏览器 1 登 `bluesurfiregpt@gmail.com` / `<SYSTEM_OWNER_PWD>`
2. 看右上角 应该 同时有 3 个圆按钮:**⚙️ + 📊 + ⚡**(红色)
3. F12 → Network → 看 `GET /api/super/me` 返回:`{"is_platform_admin":true,"email":"bluesurfiregpt@gmail.com","platform_admin_emails_count":1}`

**Pass**:三个按钮齐 + JSON `is_platform_admin: true`

### P-4 初始环境干净
1. 点 ⚡ 进 `/super`
2. workspace 表格 应该**只有 1 行**:默认工作空间(slug `default`)
3. 该行 user_count=1 / agent_count=21 / meeting_count=0

**Pass**:行数=1 + meeting_count=0(若 meeting_count>0 说明 清理不完整,**整份 BLOCKED**)

---

## 3. 真实场景 T-01 ~ T-12

### T-01 · 超管创建客户 workspace + 拿凭证

**前置**:P 全 pass,在 /super 页
**步骤**:
1. 点 **➕ 新建工作空间**
2. 填:
   - 工作空间名称:`Kimi 测试客户单位-<时间戳>`(例 `Kimi 测试客户单位-20260513200000`)
   - owner 姓名:`Kimi 客户 owner`
   - owner 邮箱:`kimi-client-<同一时间戳>@example.com`
   - 临时密码:**留空**(让系统自动生成)
   - ☑️ 生成一次性邀请链接
   - ☐ 不勾 seed demo(避免 60s 等待)
3. 点 ✓ 创建
4. 应在 < 3 秒看到 emerald 回执屏

**Pass**(全部):
- 标题字面含 `✓ workspace 「Kimi 测试客户单位-...」已创建`
- 显示 owner 邮箱(字面 = 你填的)
- 显示临时密码(字面 ≥ 24 字符,随机串)
- 显示一次性邀请链接(字面以 `https://aimeeting.zhzjpt.cn/register?invite=` 开头)
- 点"复制"按钮 toast 字面 `已复制邀请链接`

**必须复述**(后续要用):
```
WORKSPACE_NAME = <从屏幕复述>
OWNER_EMAIL    = <从屏幕复述>
TEMP_PASSWORD  = <从屏幕复述,完整 24+ 字符>
INVITE_URL     = https://aimeeting.zhzjpt.cn/register?invite=<token>
```

---

### T-02 · 客户激活 — 邀请链接走通

**前置**:T-01 pass,凭证记下来
**步骤**:
1. 关闭回执屏(点 我已记下,关闭)
2. 在 **隐私窗口/不同浏览器** 打开 `INVITE_URL`
3. 页面 应跳 `/register?invite=...`,显示 `加入工作空间「Kimi 测试客户单位-...」`
4. 填:
   - 姓名:`Kimi 客户 owner`
   - 邮箱:`OWNER_EMAIL`
   - 密码:**新密码** `ClientPass123!`(不是临时密码 — 客户的真实密码)
5. 注册 → 自动跳 `/`

**Pass**:
- 注册 status 200
- 跳到 `/` 后 顶栏 显示 `<客户 workspace 名> · Kimi 客户 owner`
- `GET /api/auth/me` 返回:
  - `role: "owner"`
  - `workspace_id` 字面 = T-01 创建的 workspace_id(可在 /super 列表对照)
  - `email` 字面 = OWNER_EMAIL

**必须复述**:`GET /api/auth/me` 完整 JSON

---

### T-03 · 客户尝试访问 /super — `[权限不足]` 验证 ★

**前置**:T-02 pass,客户 owner 已登录(隐私窗口)
**步骤**:
1. 在客户 owner 的浏览器,地址栏直接输 `https://aimeeting.zhzjpt.cn/super`
2. 等 ~2 秒,看 toast

**Pass**(全部):
- 出现 **🟡 黄色 toast**,标题字面 = **`权限不足`**(**不是** "请求失败 (403)"、**不是** 英文)
- toast detail 字面含 `email kimi-client-<...>@example.com 不在 PLATFORM_ADMIN_EMAILS 白名单`
- 页面显示 `⚡ 无平台超管权限` 拒绝页
- 1.5 秒后 自动跳到 `/`

**Fail**:
- 看到 "请求失败 (403)" toast → fail("v26.4-fix4 前端 prefix 解析失效")
- toast 是其他颜色(红 / 蓝) → fail("toast 类型 mapping 错")
- 没跳转 → fail("前端 redirect 失效")

**必须复述**:toast 完整文字 + 截图

---

### T-04 · 超管 切换进 客户 workspace

**前置**:T-02 pass · T-03 pass · 超管浏览器仍在
**步骤**:
1. 回 超管浏览器(浏览器 1)
2. 点 ⚡ → /super
3. 列表现在应该 **2 行**:默认 ws + 新创建客户 ws
4. 看 客户 ws 一行 user_count=1 / agent_count=0 / meeting_count=0 / status=`active`
5. 点 客户 ws 行的 **"进入 →"** 按钮
6. confirm 弹窗 → 确定
7. 自动跳 `/`

**Pass**(全部):
- 跳后顶栏 字面 `<客户 workspace 名> · Bluesurfire`(不是 "默认工作空间")
- ⚡ 仍在(超管身份不随 ws 切换丢)
- **没有任何 toast 错误**(关键!验证 fix1/2/3)
- ⚙️ 后台 + 📊 看板 入口都在(验证 fix1)
- `GET /api/auth/me` 返回:`workspace_id` = 客户 ws id,`role: "owner"`(fix1 让 super admin 跨 ws 自动 owner role)

**Fail**:
- 看到 `请求失败 (403)` / `not a member` / `权限不足` 任意 toast → fail("fix2 没生效")
- ⚙️ 按钮消失 → fail("me 返回 role=member 没 fallback to owner")

**必须复述**:`GET /api/auth/me` 完整 JSON + 顶栏截图

---

### T-05 · 超管 在 客户 ws 内 浏览 admin

**前置**:T-04 pass,超管现处于 客户 ws 视角
**步骤**:
1. 点 ⚙️ 后台 → 进 `/admin/agents` (AI 专家)
2. 应正常加载(客户 ws 是新建空 ws,agent 列表应空)
3. 切到 `/admin/team` (团队)
4. 应正常加载(只 1 行客户 owner)
5. 切到 `/admin/demo-data`

**Pass**(全部):
- 三个页面 **都不报 toast** 错误
- agent 列表显示 0(或对应空状态文字)
- team 列表显示 1 行:`OWNER_EMAIL`,role=`owner`,姓名 `Kimi 客户 owner`
- 控制台 (F12) 没有红色 4xx error

**Fail**:任一页面出 toast → 粘出 toast 文字 + URL

---

### T-06 · 超管 试 焦土清除 — `[操作受限]` 验证 ★

**前置**:T-05 pass,超管在 客户 ws 视角的 `/admin/demo-data` 页
**步骤**:
1. 滚到页面底部 找 🔥🔥🔥 焦土重置 区域(用户提示文字 应 出现 "只 owner 能用")
2. 焦土区域输入框 输 `SCORCH`(大写)
3. 点焦土按钮 → confirm 弹窗 → 确定

**Pass**(全部):
- 出现 **🔵 蓝色 info toast**,标题字面 = **`操作受限`**(不是 黄 warn,不是 红 error)
- toast detail 字面含 `焦土数据清除仅 workspace owner 可操作` + `平台超管也不代操作,安全设计`
- HTTP status 字面 = `403`

**Fail**:
- toast 标题是 "权限不足"(黄)→ fail("分类错 — 这是设计性拒绝应该是 操作受限 蓝)
- 焦土真跑了(列表清空)→ fail("严重:超管绕过了客户 ws owner 检查!安全漏洞!")

**必须复述**:toast 截图 + Network 中 `/api/dashboard/scorched-earth-reset` 返回的 body

---

### T-07 · 超管 切回 默认 workspace

**前置**:T-04~T-06 pass
**步骤**:
1. 顶栏 ⚡ → 重新进 /super
2. 列表里 找 "默认工作空间" 一行 → 点 "进入 →" → 确定
3. 跳 `/`

**Pass**:
- 顶栏 字面 `默认工作空间 · Bluesurfire`
- `GET /api/auth/me` 返回 `workspace_id` = `bfaf52e4-ba42-4eb6-9577-20a1ffcd4a55`
- 默认 ws 内 agent 数仍 = 21,meeting 数 = 0(数据没串)

**必须复述**:`/api/auth/me` JSON

---

### T-08 · audit 留痕验证 — 超管访问 客户可见

**前置**:T-04 / T-06 已发生 audit 事件
**步骤**:
1. **隐私窗口** 回 客户 owner 视角(用 T-02 设的密码 ClientPass123! 登)
2. 进 `/admin/audit`
3. 看 audit 列表

**Pass**:
- 至少看到 1 条 action = `workspace.switch_in`(超管切入)
- 该 audit row 的 user_id / 操作者 应是 super admin 的 user_id(bluesurfire 的 id `12728da4-...`)
- 该 audit row 的 payload 字面含:
  - `"platform_admin": true`
  - `"platform_admin_email": "bluesurfiregpt@gmail.com"`

**Fail**:
- 找不到 `workspace.switch_in` audit → fail("fix2 super.py 写 audit 失败")
- payload 没 platform_admin flag → fail("audit_log 没自动打 flag")

**必须复述**:该 audit row 的完整 payload JSON

---

### T-09 · 客户 创建第一个 hybrid 会议 — Happy path

**前置**:T-08 pass,在 客户 owner 隐私窗口
**步骤**:
1. 回首页 `/`
2. 创建会议表单:
   - 标题:`Kimi 综合测试-客户首会议`
   - 模式:`真人 + AI 混合(默认)` (auto 模式 客户应能选 — 但 客户 ws 还没 agent,本场测 hybrid)
   - 议程:留空(hybrid 不必填)
3. 点 开始会议

**Pass**:
- 会议创建 200
- 跳 `/meeting/<id>` 会议页
- 顶栏仍是 客户 ws 名字

**Fail**:任何 403 → fail("客户 owner 在自己 ws 内 创建会议 应 work,fix1/2/3 ABAC 链路漏")

---

### T-10 · 客户 试 创建 auto 会议 — agent 数不够 → `[操作受限]` 验证

**前置**:T-09 pass
**步骤**:
1. 回首页(或新开会议)
2. 选模式 `🤖 AI 自主会议(v26.3)`
3. 填议程 2 项 + 不勾任何 AI 专家(客户 ws 还没 agent)
4. 点创建

**Pass**(任一即可):
- **方案 A(前端拦)**:点击创建按钮 前端 报错 "auto 模式至少需要 3 个 AI 专家",**没有 HTTP 请求 发出**
- **方案 B(后端拦)**:HTTP 请求发出 → 400,前端弹 🟡 黄 warn toast,标题字面 `请求失败` 或类似,detail 字面含 `至少邀请 3 个 AI 专家`

**Fail**:
- 会议创建成功 → fail("校验漏")
- toast 标题是英文 → fail
- 客户(owner)看到 `[权限不足]` toast → fail("此场景不是权限不足,是业务校验")

---

### T-11 · 跨 workspace 隔离验证

**前置**:T-09 客户 ws 创建了一个 hybrid 会议
**步骤**:
1. 切到 **超管浏览器**(浏览器 1)
2. 此时顶栏应是 `默认工作空间`
3. 访问 `/`,看 会议列表
4. 调 `GET /api/meetings` 看返回

**Pass**:
- 会议列表 **空**(不应看到 T-09 客户 ws 创建的 `Kimi 综合测试-客户首会议`)
- `GET /api/meetings` 返回数组长度 = 0(默认 ws 内确实没会议)

**Fail**:
- 看到客户 ws 的会议 → fail("workspace 隔离失效,严重数据泄露")

**必须复述**:`GET /api/meetings` 返回的 JSON

---

### T-12 · 错误提示分类 速测(综合 v26.4-fix4)

**前置**:之前所有 T 已跑
**步骤**(任意顺序 跑 3 个场景,看 toast):

| 触发场景 | 期望 toast 标题 | 期望颜色 | 期望 detail 关键词 |
|---|---|---|---|
| 客户 owner 调 `/api/super/workspaces`(curl 带 cookie) | `权限不足` | 🟡 黄 | `仅平台超管可见` / `PLATFORM_ADMIN_EMAILS` |
| 超管 在 客户 ws 视角 试焦土清除 | `操作受限` | 🔵 蓝 | `焦土数据清除仅 workspace owner` |
| (可选)用 expert 账号 试 接别人的 任务 | `资源保护` | 🟡 黄 | `仅任务负责人` |

**Pass**:三个场景 任意 2 个 满足 toast 标题中文 + 不是 "请求失败 (403)"

**必须复述**:每个场景的 toast 标题 + detail 完整文字

---

## 4. 报告模板

```markdown
# v26.4 真实场景 Kimi 报告

> 时间:<...>
> 入口:https://aimeeting.zhzjpt.cn
> 测试者:Kimi (4 浏览器:超管 + 客户owner + (可选)客户expert + (可选)客户member)

## 测试中创建的资源(便于清理 / 复述)
- 客户 workspace:<name> id=<uuid>
- 客户 owner:<email>
- 临时密码:<32 chars>
- 邀请链接:<full url>
- 客户首会议:id=<uuid>

## 总结
- 预检 4/4
- 用例 12 个 → N pass / N fail / N blocked
- 结论:GREEN / YELLOW / RED

## 关键场景验证
- ⚡ 平台超管端到端:T-01 / T-04 / T-07 / T-08
- v26.4-fix4 错误分类:T-03 (权限不足🟡) / T-06 (操作受限🔵)
- 跨 ws 隔离:T-11

## 用例明细
(每个 4 段)

## 异常/观察
(意外的 toast / UI 卡顿 / 控制台 error 等)
```

---

## 5. 反幻觉自检

- T-01 凭证 我有没有 完整 字面复述(32 字符密码 + 完整 invite URL)?
- T-04 我有没有 真的 看到 顶栏 名字变了(截图证据)?
- T-06 toast 是 蓝色 info 还是 黄色 warn?**颜色错就是 bug**.
- 三个浏览器 cookie 有没有 混用?(必须 隔离)
- "应该 / 通常 / 估计" 出现 → 报告作废

---

## 6. 已知不在范围

- v26.3 召集人模式 / auto 会议端到端(本测试 客户 ws 没 seed agent,跑不了 auto)
- demo seed 流程(本测试 客户 ws 没 seed)
- workspace suspend / archive / delete(留 v26.5)

**END.**
