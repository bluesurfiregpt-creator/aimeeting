# 智慧住建 AI 集群系统 · 完整测试用例

> 适配版本:**v25-5 已 deploy**(2026-05-10 14:32+ build)
> 测试入口:https://aimeeting.zhzjpt.cn
> 主账号:`bluesurfiregpt@gmail.com / aimeeting123`
> 演示账号(密码全部 `demo123`):
> - leader:`demo.lijg@futian.gov.cn`(局长)
> - admin:`demo.chensy@futian.gov.cn`(物业科长)
> - expert:`demo.fengl@futian.gov.cn`(物业 expert,bound AI-08)
> - member:`demo.hanx@futian.gov.cn`(物业普通员工)
>
> **总用例数:65 + 内嵌 Cowork 162**
>
> 测试工具建议:Chrome MCP(`mcp__Claude_in_Chrome__*`),所有用例都在浏览器里完成。
> 一致性检查:每个用例做完后看 控制台 是否有红色 error / 网络面板是否有 5xx。

---

## 0. 测试前置 准备

### 0.1 浏览器环境
- Chrome 最新版,启用 Chrome MCP 扩展
- 关掉广告拦截 / Tampermonkey 等插件(避免误报)
- 一个 incognito 窗口 + 一个 普通窗口(切多账号用)

### 0.2 服务器健康检查(SSH 不必要,curl 即可)
```bash
curl https://aimeeting.zhzjpt.cn/healthz
# 期望:{"ok":true,"env":"prod","sentry_active":false}
```

### 0.3 强制 Cowork 自检脚本
F12 控制台 → 拷贝运行(下面 A-2 测;此处仅熟悉)
```js
const r = await runCoworkSuite();
console.log(r.markdown);
```

---

## A · 一键准备 + 自动化基线(3)

### A-1 wipe 清除测试数据
**前置**:主账号 `bluesurfiregpt@gmail.com` 登录(密码 `aimeeting123`)
**步骤**:
1. 导航 https://aimeeting.zhzjpt.cn/admin/demo-data
2. 红色卡 输入框 输入 `WIPE`(大写4字母)
3. 点击 「⚠️ 立即清除」按钮
4. 浏览器 confirm 对话框 → 点确定
5. 等待 toast 出现

**预期**:
- ✅ Toast 出现 `✅ 已清除 N 行数据`
- ✅ 卡片下方出现 行数表(包括 task/meeting/agent/...)
- ✅ 无 5xx,控制台无 error
- ✅ N ≥ 0(干净环境 N=0 也算 pass)

---

### A-2 一键 seed 演示场景
**前置**:A-1 完成
**步骤**:
1. 同页面紫色卡 「🎬 一键 seed 演示场景」
2. 「灌入 48 篇 KB 文档」checkbox 保持 ✓
3. 点 「🎬 一键 seed 演示场景」
4. 浏览器 confirm 对话框 → 点确定
5. 等待 30-60 秒(KB embedding 慢)

**预期**:
- ✅ Toast 出现 `✅ 演示场景已就绪`
- ✅ Summary 表显示(数字偏差 ±2 OK):
  - `agents`: 16
  - `users_created`: 19(或 `users_skipped`: 19 如多次 seed)
  - `meetings`: 10
  - `transcripts`: ≥ 50
  - `agent_messages`: ≥ 10
  - `action_items`: ≥ 5
  - `tasks`: 30
  - `upper_docs`: 5
  - `directives`: 5
  - `kb_documents`: 48
  - `kb_chunks`: ≥ 80
  - `embed_failed`: 0
- ✅ 无 5xx,控制台无 error

---

### A-3 跑 Cowork 自动化基线(162 用例)
**前置**:A-2 完成,F12 控制台打开
**步骤**:
1. F12 → Console
2. 粘贴并运行:
```js
const r = await runCoworkSuite();
console.log(r.markdown);
console.log("REGRESSIONS:", r.diff?.regressions?.length || 0);
console.log("FIXED:", r.diff?.fixed?.length || 0);
console.log("PASS/TOTAL:", r.json.summary.pass + "/" + r.json.summary.total);
```
3. 等待 ~3-5 分钟(全部用例跑完)

**预期**:
- ✅ `total`: 162 (允许多 1-2 = 后续追加)
- ✅ `pass`: ≥ 152(预期 154-159 之间;XX-2 LLM 配额可能预留)
- ✅ `fail`: 0
- ✅ `skipped`: 8 (B/C/D/W-12/13/X-22/23/24 — 都是 ASR 真人 / 浏览器麦克权限相关)
- ✅ `regressions: []`
- ✅ markdown 报告里 无 ❌

⚠️ 若 fail > 0:复制 markdown 全文 + JSON 截图,直接当作 bug 报。

---

## B · 主账号(owner)后台 + Audit(5)

### B-1 audit log 含 wipe / seed 两条
**前置**:A-1 + A-2 已跑
**步骤**:
1. 导航 https://aimeeting.zhzjpt.cn/admin/audit
2. 找 action 列含 `workspace.wipe_demo_data` 和 `workspace.seed_demo_scenario`

**预期**:
- ✅ 两条记录都在
- ✅ 时间戳 ≤ 5 分钟前
- ✅ payload 列展开能看到 `total`(wipe)和 `agents/users/...`(seed)统计

---

### B-2 healthz 暴露 sentry_active
**步骤**:
1. F12 → Console
2. 运行 `await fetch("/healthz").then(r => r.json())`

**预期**:
- ✅ 返回 `{ ok: true, env: "prod", sentry_active: false }`(DSN 没填时 false)

---

### B-3 LLM 配额状态查询
**步骤**:
1. F12 → Console
2. 运行 `await fetch("/api/me/llm-quota/status", {credentials:"include"}).then(r=>r.json())`

**预期**:
- ✅ 返回包含 `user_used / user_limit / workspace_used / workspace_limit / window_seconds`
- ✅ `user_limit`: 30
- ✅ `workspace_limit`: 200
- ✅ `window_seconds`: 60

---

### B-4 演示数据 tab 在 admin layout
**步骤**:
1. 导航 https://aimeeting.zhzjpt.cn/admin/agents
2. 看顶部 nav 是否有「演示数据」+「访问申请」tab

**预期**:
- ✅ nav 包含 `AI 专家 / 知识库 / LLM 模型 / 长期记忆 / 定期巡检 / 团队 / 访问申请 / 操作日志 / 演示数据` 9 个 tab(或多1-2个)

---

### B-5 操作日志列表 排序 + 限制
**步骤**:
1. https://aimeeting.zhzjpt.cn/admin/audit
2. 看时间排序

**预期**:
- ✅ 最新在最上
- ✅ 至少 5 条记录(seed 操作会写一堆)
- ✅ 每条含 `action / actor / target / payload / time`

---

## C · 局长(leader)视角(8)

### C-1 leader 登录 + 主屏
**前置**:浏览器隐私窗口(切账号用)
**步骤**:
1. 隐私窗口打开 https://aimeeting.zhzjpt.cn/login
2. `demo.lijg@futian.gov.cn / demo123` 登录
3. 跳转 / 主页

**预期**:
- ✅ 主页显示「让会议拥有记忆与专家」title
- ✅ 顶部 nav 5 项:`首页/会议/我的/看板/...` 类似
- ✅ user 下拉显示 `李建国`

---

### C-2 看板 7 KPI
**步骤**:
1. 顶 nav 「看板」 → /dashboard

**预期**:
- ✅ 顶部 4 张 KPI 卡:总任务 / 待审 / 已逾期 / 本月完成率
- ✅ 总任务 = 30(seed 数量)
- ✅ 状态饼图(StatusPie)出现,有 ≥ 4 种状态色块
- ✅ 来源饼图(SourcePie)出现,含 meeting / leader_directive / upper_doc 三种
- ✅ 部门负载柱图(workload)出现
- ✅ 顶部「↻ 刷新」按钮可点

---

### C-3 自然语言问数(AI 听懂中文)
**步骤**:
1. /dashboard/ask
2. 输入框输入 `按状态看任务`
3. 点提交

**预期**:
- ✅ 5-15 秒后 出现 饼图
- ✅ 图下显示 模板名(`status_pie` 类似)
- ✅ 数据点 ≥ 3 个(对应任务状态分布)
- ✅ 无 fallback_used: true(LLM 选模板成功)

附加测:输入 `过去 30 天每天新建多少` → 应出 折线图(line)

---

### C-4 趋势分析 3 sparkline
**步骤**:
1. /dashboard/trends

**预期**:
- ✅ 3 张卡:任务新建 / 任务完成 / 逾期率
- ✅ 每卡含 sparkline + 大数字 + trend label(上升/下降/平稳)
- ✅ 至少 1 张卡 不显示「样本不足」(seed 数据应足够)

---

### C-5 报表 4 项导出
**步骤**:
1. /dashboard/reports
2. 4 张紫卡:日报 / 周报 / 月评 / 月报
3. 点「日清」→ 等下载

**预期**:
- ✅ 4 张紫卡都出现
- ✅ 点「日清」触发 .xlsx 下载,文件名包含中文 `日清` 或 `daily-summary` + 日期
- ✅ 文件不空(>1KB)
- ✅ 用 Excel / Numbers 打开能看到 ≥ 1 行数据
- ✅ Sheet 数 ≥ 2

---

### C-6 AI 看板(按 agent 分组)
**步骤**:
1. /dashboard/kanban-agents

**预期**:
- ✅ 看到 16 列(对应 16 AI),或当前有任务的 N 列
- ✅ 每列含 「N 项 · M 逾期」摘要
- ✅ 卡片颜色按 agent 颜色(emerald/violet/sky 等)

---

### C-7 用户看板(按 user 分组)
**步骤**:
1. /dashboard/kanban-users

**预期**:
- ✅ 列 = 派给 demo 用户的 task 分组(冯磊 / 王璐 / ...)
- ✅ 「未指派」列存在(部分 task 没 assignee 的话)

---

### C-8 团队管理 + ABAC
**步骤**:
1. /admin/team
2. 看成员表

**预期**:
- ✅ 19 demo 用户 + 主账号都列出
- ✅ 每行有:姓名 / role(badge 不同颜色)/ department / bound AI(若 expert)
- ✅ Department 列 含 5 部门(机关党委 / 法制 / 房地产 / 物业 / 建筑业 等)
- ✅ Expert 行 显示绑定的 AI 名字
- ✅ 编辑按钮 `✎` 可点(打开 inline form)

---

## D · 16 AI Agent 配置 + KB(5)

### D-1 16 AI 列表
**步骤**:
1. /admin/agents

**预期**:
- ✅ 16 张 Agent 卡(头像 + 名字 + 业务边界)
- ✅ 每张含 ✏️ 编辑按钮
- ✅ 颜色 ≥ 8 种(sky / emerald / violet / rose / amber / cyan / lime / fuchsia)
- ✅ 顶部 banner 显示 「🏗️ 智慧住建已 seed」(若已 seed)

---

### D-2 单个 Agent 详情(persona / boundary / KB 绑定)
**步骤**:
1. /admin/agents 点 「物业监管 AI 专家」 → 编辑

**预期**:
- ✅ Modal 显示 persona / tone / boundary / keywords / 颜色 / KB 绑定
- ✅ keywords 含 "物业, 维修资金, 小散工程, 业委会, 物业管理"
- ✅ KB 绑定 = 「KB · 物业监管 AI 专家」一项

---

### D-3 KB 列表 16 项
**步骤**:
1. /admin/knowledge

**预期**:
- ✅ 16 个 KB(每个 AI 1 个)
- ✅ 每个 KB 卡显示 文档数 ≥ 3
- ✅ 没有 "没有 KB" 空态

---

### D-4 单个 KB 文档 + chunks
**步骤**:
1. /admin/knowledge → 点 「KB · 物业监管 AI 专家」
2. 看文档列表

**预期**:
- ✅ 3 篇文档:维修资金审批管理办法 / 小散工程纳管细则 / 物业服务企业行业指导标准
- ✅ 每篇 status = `ready`
- ✅ 每篇 chunk_count ≥ 1
- ✅ char_count > 200

---

### D-5 文档智能审核(v24.2 #3)
**步骤**:
1. /admin/document-audit
2. 粘贴一段公文(可用 D-4 里某文档内容)
3. 点 「审核」

**预期**:
- ✅ 5-15 秒返回审核结果
- ✅ 显示 `format / wording / policy` 三维 issues 列表
- ✅ 有 `overall` 总评
- ✅ 无 `fallback_used: true`(LLM 成功)

---

## E · 会议详情 + AI 发言 + 完整纪要(6)

### E-1 会议列表
**步骤**:
1. /meetings

**预期**:
- ✅ 10 场会议
- ✅ 5 finished / 2 ongoing / 3 scheduled (大致)
- ✅ 每条:标题 / 状态 / 时间 / 参会人

---

### E-2 finished 会议详情(物业纠纷调解)
**步骤**:
1. /meetings → 点 「12月物业纠纷调解专题会」
2. 滚动看全部

**预期**:
- ✅ Header 显示标题 + 已结束 badge + 起止时间
- ✅ 「会议纪要」section 显示 markdown 渲染
- ✅ 「实录」section 显示 4-8 行,每行 `[mm:ss] 姓名: 内容`
- ✅ 「AI 发言」section 显示「物业监管 AI 专家」发言 + 紫色 chip
- ✅ 鼠标悬停 chip → tooltip 显示 KB 文档原文片段
- ✅ 「待办事项」section 显示 ≥ 1 条 action item

---

### E-3 完整纪要 docx 导出(v25-5 重头戏)
**前置**:E-2 在同一会议页面
**步骤**:
1. 滚到 SummaryCard
2. 点紫色 「📄 完整纪要」按钮

**预期**:
- ✅ 触发下载 `会议纪要-XXX-YYYYMMDD.docx`
- ✅ 文件大小 ≥ 5KB
- ✅ 用 Word / Pages / 在线 office 打开
- ✅ 看到 9 节:标题 / 一、会议信息 / 二、议程(若有) / 三、参会人员 / 四、会议摘要 / 五、会议记录 / 六、AI 专家发言 / 七、待办事项 / 页脚
- ✅ 标题居中
- ✅ 「五、会议记录」每段 `[mm:ss] 姓名:` 蓝色加粗
- ✅ 「六、AI 专家发言」每段 `【AI 名】` 紫色加粗
- ✅ 「七、待办事项」是编号列表,带 「负责人 / 截止 / 状态」

---

### E-4 简版导出 .md / .docx
**步骤**:
1. 同会议页面
2. 点「导出 .md」

**预期**:
- ✅ 下载 .md 文件
- ✅ 内容含 `# 会议标题` + `## 会议纪要` + `## 实录` 等

3. 再点「导出 .docx」(简版)
**预期**:
- ✅ 下载 .docx,内容比 完整纪要 简洁(没议程 / 没 AI 发言 / 没待办)

---

### E-5 ongoing 会议详情(燃气安全整治督办会)
**步骤**:
1. /meetings → 点 「燃气安全整治督办会」(ongoing)

**预期**:
- ✅ 状态 badge 显示「进行中」
- ✅ 没结束时间
- ✅ 「会议纪要」section 显示 「生成中」 或空(尚未 finalized)
- ✅ 麦克风按钮 / 实时字幕 区域显示(若启用麦克风权限)
- ⚠️ 该用例不要求真录音,只验 UI 状态

---

### E-6 议程监控 + agent 推荐
**步骤**:
1. E-5 同会议
2. 看议程项(若有)

**预期**:
- ✅ 议程项编号 + 时间预算 显示
- ✅ 「燃气安全整治督办会」议程含 `近期燃气安全形势` / `重点单位排查情况` / `下一步部署`(seed 内容)

---

## F · 任务全闭环(10)

### F-1 leader 看任务列表
**前置**:leader (`demo.lijg`) 登录
**步骤**:
1. /me

**预期**:
- ✅ Tab `我的待办 / 办理中 / 等审核 / 已办结` 都有
- ✅ 「等审核」tab 显示 ≥ 5 条(seed `submitted` 状态)
- ✅ 「已办结」tab 显示 ≥ 8 条(seed `done`)
- ✅ 每行含 标题 / status badge / 数据分级 badge / 截止时间 / 来源

---

### F-2 任务详情(完整状态)
**步骤**:
1. /me 「等审核」 tab → 点第一条进入
2. 看详情

**预期**:
- ✅ 标题 + status badge + 数据分级 badge 出现
- ✅ 「截止」时间显示
- ✅ 「来源:[meeting / directive / upper_doc]」明确
- ✅ 「主责」「派发人」「创建人」三个角色显示
- ✅ 阶段汇报 section 含 4 段(已完成 / 问题 / 下一步 / 佐证)— 若 status=submitted

---

### F-3 leader 创建领导指令 + LLM 拆解
**步骤**:
1. /me → 「领导指令」tab
2. 点 「+ 新建指令」
3. 输入 `请房屋安全科本周完成 3 栋 D 级危房应急避险措施`
4. 点 「解析」

**预期**:
- ✅ 5-15 秒后 出 1+ task drafts
- ✅ 每 draft 含 `title / assignee_name? / due_at?`
- ✅ 「派发」按钮可点
- ⚠️ 此用例不真派发,验拆解能力即可

---

### F-4 leader 派发 task 给 expert
**前置**:F-3 完成,有 draft
**步骤**:
1. F-3 同界面 点 draft 的 「派发」
2. 选 assignee 「冯磊」(物业 expert)
3. due 设 `7 天后`
4. 点确认

**预期**:
- ✅ Task 创建成功,跳转任务详情页
- ✅ 状态 = `dispatched`
- ✅ assignee = 冯磊
- ✅ /me 「派发出」 tab 多 1 条

---

### F-5 expert 接任务(切账号)
**前置**:F-4 完成
**步骤**:
1. 隐私窗口 / 新窗口 用 `demo.fengl@futian.gov.cn / demo123` 登录
2. /me 看「待签收」 tab

**预期**:
- ✅ 看到 F-4 派的任务
- ✅ Status badge `dispatched`
- ✅ 「签收」按钮可点

---

### F-6 expert 签收 + 开始 + AI 起草汇报(亮点)
**前置**:F-5 同账号
**步骤**:
1. 进任务详情
2. 点 「✅ 签收」 → status → `accepted`
3. 点 「▶️ 开始办理」 → status → `in_progress`
4. 点 「📝 提交汇报」 → 弹出 4 段模板表单
5. 在表单里点 「✨ AI 帮我写」按钮

**预期**:
- ✅ 5-15 秒 AI 自动填 3 段(已完成/问题/下一步)
- ✅ 文字非空,中文,合理
- ✅ 「提交」按钮可点
- ✅ 提交后 status → `submitted`

---

### F-7 leader 审批 + 沉淀 KB
**前置**:F-6 完成
**步骤**:
1. 切回 leader tab
2. 刷新 /me 「等审核」 tab
3. 看到 F-6 提交的 task → 点进入详情
4. 点 「✅ 批准办结」

**预期**:
- ✅ Status → `done`
- ✅ 「批准」按钮变灰
- ✅ 5-30 秒后 任务上出现 「📚 已沉淀」 绿色 badge

---

### F-8 验沉淀 KB(关键闭环)
**前置**:F-7 完成,等 30 秒
**步骤**:
1. /admin/knowledge → 找「KB · 物业监管 AI 专家」(F-4 任务对应的 AI)
2. 看文档列表

**预期**:
- ✅ 多了一篇 文档,filename 含 「办结沉淀」 或 task 标题
- ✅ status = `ready`
- ✅ chunks > 0

---

### F-9 自动派发 4 维路由(v24.1 #3)
**前置**:leader (`demo.lijg`) 登录,有 draft 任务
**步骤**:
1. /me 「领导指令」 tab,创建一条新指令含 「物业」「维修资金」 关键词
2. 点 draft 「自动派发」按钮

**预期**:
- ✅ 5-10 秒返回推荐 assignee
- ✅ 推荐人是 `冯磊`(物业 expert)— 因为 keyword 0.4 + capability 0.1 都中
- ✅ 显示推荐分 ≥ 0.30(threshold)
- ✅ 显示 4 维分解(keyword / history / load / capability)

---

### F-10 任务超时扭分 + 暂停派单(v24.3 #3)
**步骤**:
1. F12 控制台:
```js
const r = await fetch("/api/dashboard/test-fire-overdue", {method: "POST", credentials:"include"}).catch(() => null);
console.log(r?.status);
```
(此端点可能不存在,如 404 则跳过 — 不算 fail)

**预期**:
- ⚠️ 该 case 是「能跑过即可」的探测,不强求 200
- ✅ 主要看 /admin/team 是否有「🚫 暂停派单」 badge(若上次 demo 触发过)

---

## G · 领导指令 + 上级文件 + OCR(5)

### G-1 上级文件文字 PDF 上传(v20)
**前置**:leader 登录
**步骤**:
1. /me → 「上级文件」 tab → 「上传」
2. 上传任意 .pdf / .docx(可用一份带中文的 word 文件)
3. 等解析

**预期**:
- ✅ 上传成功
- ✅ status = `draft`
- ✅ extracted_text 非空(看 detail 页)
- ✅ parsed_drafts 数组 ≥ 0(LLM 拆解结果)

---

### G-2 上级文件 图片上传(v25-2 OCR)
**步骤**:
1. /me 「上级文件」 → 上传
2. 选一张你手机上的中文公文照片(.jpg / .png)
3. 等 30-60 秒(OCR 慢)

**预期**:
- ✅ 上传成功
- ✅ status = `draft` 或 `failed`(若图片无字)
- ✅ extracted_text 非空(图片含文字时)
- ✅ 控制台无 Qwen-VL 错误

---

### G-3 KB 上传 image
**步骤**:
1. /admin/knowledge → 进任意 KB(比如「KB · 房屋安全 AI 专家」)
2. 「上传文档」 → 选 .png / .jpg(含中文文字 — 比如截屏一段微信文字)
3. 等 30-60 秒

**预期**:
- ✅ 文档进入 `parsing` → `embedding` → `ready` 流程
- ✅ status = `ready` 时 chunk_count ≥ 1
- ✅ 文件 chars 非 0

---

### G-4 KB 上传扫描件 PDF
**步骤**:
1. 同 G-3 但选一份 全部是扫描图片的 PDF(无可选文字)
2. 等 1-3 分钟

**预期**:
- ✅ 走 OCR fallback(后台 log 应该有 `OCR fallback ...`)
- ✅ status = `ready`,chunks > 0

---

### G-5 KB 上传文字 PDF / DOCX(走原路径,不调 OCR)
**步骤**:
1. 同 G-3 但选 普通 PDF/DOCX
2. 等几秒

**预期**:
- ✅ 解析快(无 OCR 30-60s 等待)
- ✅ status = `ready`

---

## H · 知识库管理 边界(4)

### H-1 不支持的格式 → 400
**步骤**:
1. /admin/knowledge → 进 KB → 上传
2. 选 .exe / .zip / .mp3 等不在 SUPPORTED 列表的

**预期**:
- ✅ Toast 显示 unsupported,400 错误
- ✅ 控制台无 5xx

---

### H-2 超大文件 → 413
**步骤**:
1. 上传 50MB+ 文件

**预期**:
- ✅ 413 too large 错误

---

### H-3 KB 检索召回(F12 console)
**步骤**:
1. F12 console
```js
await fetch("/api/agent-test/retrieve?kb_id=KB_UUID&q=维修资金", {credentials:"include"}).then(r=>r.json())
```
注:可能没此端点 — 跳过即可。改为去会议中 @物业 AI 看引用 chips。

**预期**:
- ⚠️ 跳过即 OK

---

### H-4 删除 KB 文档
**步骤**:
1. /admin/knowledge → 进 KB → 找一篇文档 → 「删除」按钮

**预期**:
- ✅ 删除后列表少 1 条
- ✅ chunks 也跟着删(后台日志可见)

---

## I · 数据分级 + 跨 AI 申请(4)

### I-1 访问申请审批页(空态)
**前置**:leader 登录
**步骤**:
1. /admin/access-requests
2. filter 默认 「待审批」

**预期**:
- ✅ 显示 📋 emoji + 「目前没有待审批的访问申请」 + 引导文案
- ✅ 切到 「全部」 也是空

---

### I-2 创建一条访问申请(F12 console manual)
**前置**:leader 登录
**步骤**:
1. F12 console:
```js
// 找一个 task ID(随便一个)
const tasks = await fetch("/api/me/tasks", {credentials:"include"}).then(r=>r.json());
const tid = tasks[0]?.id || tasks.items?.[0]?.id;
// 创建申请(自己也能给自己开,虽然意义小)
const r = await fetch("/api/me/access-requests", {
  method: "POST",
  credentials: "include",
  headers: {"Content-Type": "application/json"},
  body: JSON.stringify({
    target_resource_type: "task",
    target_resource_id: tid,
    justification: "测试 v25-3 审批流"
  })
}).then(r => r.json());
console.log(r);
```

**预期**:
- ✅ 返回 `{ id, status: "pending", target_owner_user_id: ..., ... }`

---

### I-3 审批页能看到 pending(I-2 后)
**前置**:I-2 完成
**步骤**:
1. 刷新 /admin/access-requests

**预期**:
- ✅ 至少 1 条 pending 申请
- ✅ 显示 申请人 ID / 资源类型 / 创建时间
- ✅ 右侧:绿色「✅ 通过 24 小时」 + 红色「❌ 驳回(理由)」 区

---

### I-4 通过申请
**前置**:I-3 同页面
**步骤**:
1. 点 「✅ 通过」 (24 小时默认)

**预期**:
- ✅ Toast `✅ 已通过(有效 24h)`
- ✅ 行刷新,status badge 变 `已通过`
- ✅ 显示 决定时间 + 有效至

---

## J · 通知 + 后台 loop(3)

### J-1 通知 bell
**步骤**:
1. 顶 nav 点 🔔 icon

**预期**:
- ✅ Drawer 打开
- ✅ 显示通知列表(seed 后可能只有少量)
- ✅ 「全部已读」按钮可点

---

### J-2 alert 强制触发(force-check)
**步骤**:
1. F12 console:
```js
await fetch("/api/dashboard/alerts/force-check", {method:"POST", credentials:"include"}).then(r=>r.json())
```

**预期**:
- ✅ 返回 `{rules_evaluated: 3, alerts_emitted: N, ...}`
- ✅ N 可能为 0(seed 数据未触发阈值),不算 fail
- ✅ 控制台无 5xx

---

### J-3 月度评价强制运行
**步骤**:
1. F12 console:
```js
await fetch("/api/dashboard/monthly-eval/force-run", {method:"POST", credentials:"include", headers:{"Content-Type":"application/json"}, body:"{}"}).then(r=>r.json())
```

**预期**:
- ✅ 返回 `{ period: "YYYY-MM", evaluations_created: N, ... }`
- ✅ N ≥ 0

---

## K · 基础设施(3)

### K-1 LLM rate limit 主动消耗
**步骤**:
1. F12 console:
```js
const before = await fetch("/api/me/llm-quota/status", {credentials:"include"}).then(r=>r.json());
const r = await fetch("/api/me/llm-quota/check", {method:"POST", credentials:"include"}).then(r=>r.json());
const after = await fetch("/api/me/llm-quota/status", {credentials:"include"}).then(r=>r.json());
console.log({before, after, r});
```

**预期**:
- ✅ `after.user_used == before.user_used + 1`
- ✅ `r.status` not 429

---

### K-2 LLM 配额超限 → 429(快速 burn)
**前置**:重启浏览器(quota 内存,reset 不靠谱;或等 60s 自然 expire)
**步骤**:
1. F12 console:
```js
let firstReject = -1;
for (let i = 1; i <= 32; i++) {
  const r = await fetch("/api/me/llm-quota/check", {method:"POST", credentials:"include"});
  if (r.status === 429) { firstReject = i; break; }
}
console.log("First 429 at:", firstReject);
```

**预期**:
- ✅ firstReject ∈ [1, 31](理想 31 — 用户 limit 30)
- ✅ 不会全都通过(说明 quota 不工作)

---

### K-3 Cowork XX-2 自验
**步骤**:
1. F12 console:`r.json.results.find(c => c.id === "XX-2")` (前面 A-3 已跑)

**预期**:
- ✅ XX-2 status = `pass`
- ✅ evidence 含 `firstReject` ∈ [1, 31]

---

## L · 演示走查 friction(3)

### L-1 空 dashboard 引导(L-1 仅 wipe 后无 seed 时跑)
**前置**:wipe 但不 seed
**步骤**:
1. /dashboard

**预期**:
- ✅ StatusPie 空时显示「首次使用?去 演示数据 一键 seed」
- ✅ SourcePie 空时显示「先创建 / seed 任务后这里就有」

(如不想走 wipe-only state,跳过)

---

### L-2 expert 看不到非自己 AI 的资源
**步骤**:
1. expert (`demo.fengl`) 登录(物业 expert,bound AI-08)
2. /me 看任务列表

**预期**:
- ✅ 看到的任务 全部 关联 物业相关 AI 或 自己 assignee 的
- ✅ /admin/agents 看不到(expert 无后台权限)
- ✅ /admin 跳转可能 403 或 redirect

---

### L-3 演示脚本 docs 文件存在
**步骤**:
```bash
ls -la docs/客户演示脚本.md
```

**预期**:
- ✅ 文件存在,大小 ≥ 8KB
- ✅ 包含 5 幕剧 + 9 大亮点 + Q&A

---

## M · 错误处理 / 边界(5)

### M-1 未登录访问 /admin
**步骤**:
1. 注销 / 删 cookie
2. 直接访问 https://aimeeting.zhzjpt.cn/admin/team

**预期**:
- ✅ 跳转到 /login(或 401 + 提示)

---

### M-2 错误账号登录
**步骤**:
1. /login
2. `wrong@example.com / wrongpw` 登录

**预期**:
- ✅ 显示「邮箱或密码错误」类似中文提示
- ✅ 不进系统

---

### M-3 wipe-demo-data 缺 confirm
**步骤**:
1. F12 console:
```js
await fetch("/api/dashboard/wipe-demo-data", {method:"POST", credentials:"include", headers:{"Content-Type":"application/json"}, body:"{}"}).then(r=>r.status)
```

**预期**:
- ✅ 返回 400

---

### M-4 wipe-demo-data 错的 confirm
**步骤**:
```js
await fetch("/api/dashboard/wipe-demo-data", {method:"POST", credentials:"include", headers:{"Content-Type":"application/json"}, body:JSON.stringify({confirm: "no"})}).then(r=>r.status)
```

**预期**:
- ✅ 返回 400

---

### M-5 错误 task ID
**步骤**:
1. 直接访问 /task/00000000-0000-0000-0000-000000000000

**预期**:
- ✅ 显示「任务不存在 / 没权限」类型错误页
- ✅ 不 5xx

---

## 测试报告模版

跑完后把这段贴 issue:

```markdown
# v25-5 完整测试报告

**测试时间**:YYYY-MM-DD HH:MM
**测试人**:XXX
**环境**:https://aimeeting.zhzjpt.cn (build YYYY-MM-DD HH:MM:SS)

## 概要
- A 一键准备:✅ X/3 通过
- B 主账号后台:✅ X/5
- C leader 视角:✅ X/8
- D 16 AI:✅ X/5
- E 会议 + 纪要:✅ X/6
- F 任务全闭环:✅ X/10
- G 上级文件 + OCR:✅ X/5
- H KB 边界:✅ X/4
- I 数据分级:✅ X/4
- J 通知 + loop:✅ X/3
- K 基础设施:✅ X/3
- L 演示走查:✅ X/3
- M 错误处理:✅ X/5
- **Cowork (A-3 内嵌)**:X/162

**总分:X/65 +Cowork**

## ❌ 失败用例
(每条:用例 ID + 实际现象 + 截图链接 + 预期与实际差异)

## ⚠️ 警告 / 边缘情况
(主观感受、性能、UX 不顺的地方)

## 建议
(下一步 sprint 优先级)
```

---

## 附录 · Claude Code 测试自动化 提示词

如果你打算让 Claude Code(CLI Agent)跑这套用例,可以这么提示:

> 我有一份测试用例 docs/测试用例.md.请用 Chrome MCP(`mcp__Claude_in_Chrome__*`)逐项执行,
> 每完成一节做 ✅/❌ 标记,并在最后输出测试报告(按文件末尾的「测试报告模版」)。
> 失败的用例用 screenshot 工具截图存到 /tmp/test-screenshots/.
> 不要修改任何代码 — 只测试,不改实现.
> 跑到 fail > 5 时停下,先汇报让我看.

---

_本文档对应 v25-5 (build 2026-05-10 14:32+).代码改动后请同步更新对应章节._
