# v27.0-mobile · Phase 3 TODO

记录 Phase 2-next 收尾时遗留的下一阶段功能, 待用户确认设计后真做.

## 1. 单专家详情页 `/m/agents/[id]`

来源: 用户反馈 (2026-05-18)
> 「补充修改: 专家视角, 在外层的数理卡片, 我觉得看到的是专家最近参加的几次会议,
> 现在归属的专家有几项任务, 完成情况什么样, 应该是用一些比较小的卡片去呈现这部分信息;
> 点击详情页后, 应该是上述信息的展开和详细说明的状态 (你还没设计, 请记录)」

### 当前状态
- 列表页工卡 = 摘要 (recent_meetings 3 条 + tasks 三个数字)
- 点击仅 `alert("Phase 3: 跳 /m/agents/${id}")`, 详情页未实现

### Phase 3 详情页应包含
1. **顶部** — 专家档案
   - 大头像 / 色块 + nickname + name + domain
   - 累计 N 场 · M 条智囊产出
   - last_active 时间
2. **「会议」tab** — 全部参与的会议
   - 列表, 每条: 会议标题 / 时间 / 该专家在此会的发言条数 / 产出 insight 数
   - 点进去 → /m/meetings/[id] 单场详情
3. **「任务」tab** — 全部归属任务 (assignee_agent_id)
   - 按 open/done/overdue 分组
   - 每条: 任务标题 / 来源会议 / 截止时间 / 状态 chip
   - 点击 → 跳任务详情 (待定路径)
4. **「智囊」tab** — 该专家的所有 AIInsight
   - 复用 InsightTopicGroupRow 按议题聚合
   - 入口: 顶部 chip "查看智囊"

### 后端需要的 endpoint
- `GET /api/m/agents/{id}` — 单专家档案 + 三 tab 数据聚合
  - 或拆 `GET /api/m/agents/{id}/meetings` / `tasks` / `insights` 三个

### 前端文件
- `frontend/src/app/m/agents/[id]/page.tsx` (新)
- 复用: `InsightTopicGroupRow`, `SegmentControl`, `PageHeader`

## 2. 其他 Phase 2-next 未做项

- `/m/insights` 三 tab 真做 (AI产出 / 待我审 / 已入库)
- CTA 接真 API (推进议程 / 召AI / 通过审核 / 通过驳回)
- `/m/meetings/[id]` sticky 底部 CTA (现 alert 占位)
- 「智囊」tab 内多专家协作视图 (一条议题多专家观点的对照展示)
