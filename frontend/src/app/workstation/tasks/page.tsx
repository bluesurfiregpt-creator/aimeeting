import { TasksPane } from "@/components/web/workstation";

// R5.C: Mira priority banner + segmented (全部/我的/AI 派的/已完成) + task detail modal
export default function TasksRoute() {
  return <TasksPane />;
}
