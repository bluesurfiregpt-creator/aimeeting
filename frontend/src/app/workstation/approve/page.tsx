import { ApprovePane } from "@/components/web/workstation";

// R5.C: 紫 Mira banner + segmented (待审/已通过/已驳回) + 三类卡 (memory/permission/workspace)
export default function ApproveRoute() {
  return <ApprovePane />;
}
