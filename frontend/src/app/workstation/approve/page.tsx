import { PlaceholderPane } from "@/components/web/workstation";

// R5.C: 紫 Mira banner + 三类 (候选记忆 / 权限申请 / 跨域引用)
export default function ApprovePane() {
  return (
    <PlaceholderPane
      title="审批中心"
      sub="候选记忆 / 权限申请 / 跨域引用 — 一键 通过 或 驳回"
      icon="check"
    />
  );
}
